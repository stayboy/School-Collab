@* Test fixture for StudentFormFieldsActionsPlacementBunitTests. Hosts
   the shared <StudentFormFields> component with a minimal model + a
   no-op submit handler so the form can render in the bUnit DOM. The
   test class toggles the ActionsPlacement parameter and asserts on
   the rendered CSS classes (sidebar modifier present vs. absent,
   form-actions--sidebar class present vs. absent, layout wrapper
   always present). The fixture has no business logic — the real
   StudentFormFields is the system under test. *@
@using SchoolCollab.Students.Admin.Components.Students
@using SchoolCollab.Students.Core.DTOs

<StudentFormFields Model="@_model"
                   OnValidSubmit="@_onSubmit"
                   SubmitLabel="Save"
                   SubmittingLabel="Saving…"
                   ActionsAlignment="ActionsAlignment"
                   ActionsPlacement="Placement" />

@code {
    [Parameter] public StudentFormFields.StudentFormActionsAlignment ActionsAlignment { get; set; } = StudentFormFields.StudentFormActionsAlignment.Right;
    [Parameter] public StudentFormFields.StudentFormActionsPlacement Placement { get; set; } = StudentFormFields.StudentFormActionsPlacement.Bottom;

    private readonly StudentFormModel _model = new();
    private static readonly EventCallback _onSubmit = EventCallback.Empty;
}
