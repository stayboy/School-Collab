# AR-10 Worker Report — WS-B2 AI extensions (round ar-10-ai-extensions)

## Mode / provenance

- **Execution mode (owner decision 2026-09-14): SOLO** on the current session model `ollama-cloud/deepseek-v4.1-flash`. Parent implements; the four-agent role contracts are self-applied by the parent. No subagent worker dispatched.
- **Round base:** `main` @ `d6d544ce` (squash-merge of PR #231 / ar-9). Branch `stack/10-ar-10-ai-extensions`, registered via `gh stack init` (local-only; nothing pushed).
- **Pass 1 of 4** (budget: P1 = steps 1–2, P2 = 3–4, P3 = 5–6, P4 = 7–8). **Pass 2 cut at the step-3 boundary** (cut-line discipline: step 3 green + build-coherent; large round, session budget consumed — step 4 is the next dispatch's lead).
- Round doc: `documents/rounds/round-ar-10-ai-extensions.md` (parent-owned; not edited by this report's author beyond the parent's own prep edits).

## Step status

| Step | Description | Status |
|---|---|---|
| 1 | Settings backend: `TenantAssignmentAiPrompt` entity + config + DbContext + DTO + CQRS (get/upsert) + routes + endpoints + Program map + additive migration + tests | ✅ COMPLETE |
| 2 | Admin surface: `AssignmentAiPromptApiClient` + ModuleServices registration (tenant propagation) + `/assignment-ai-prompt` page + Settings dashboard entry + page tests | ✅ COMPLETE |
| 3 | AI seam: request DTO fields + service validation + org-prompt layering + `TenantAssignmentAiPromptProvider` + AI.Server DI + endpoint XML doc + `propagateTenant` flip | ✅ COMPLETE |
| 4 | Assignments domain + contracts: difficulty columns + `QuestionsDraftJson` + domain methods + `InvalidQuestionsDraftException` + DTO threading + migration + `Edit.razor` round-trip + tests | ✅ COMPLETE (526/0 Assignments) |
| 5 | Draft CQRS + routes + `IAiPromptPolicyResolver`/`AiPromptPolicyResolver` + ApiClient methods | ✅ production (DONE) — 40-step-5 backend; ⏳ handler/resolver/route tests deferred to freeze |
| 6 | Wizard surfaces: form-model fields + `ResourceUrlRow` + `ResourcesSection` URL block + `UrlTextExtractor` (+CPM) + `QuestionGenerationSection` + `Create.razor` lock load | ✅ COMPLETE (see Pass-6) |
| 7 | Draft UI: `GeneratedQuestionMapper` + `QuestionsDraftSection.razor` + `Edit.razor` wiring | ✅ COMPLETE (see Pass-7) |
| 8 | Full matrix + freeze | ⏳ pending (pass 4) |

## Pass-1 files

**Created (10):**

- `src/Settings/SchoolCollab.Settings.Core/Domain/TenantAssignmentAiPrompt.cs`
- `src/Settings/SchoolCollab.Settings.Core/Data/Configurations/TenantAssignmentAiPromptConfiguration.cs`
- `src/Settings/SchoolCollab.Settings.Core/DTOs/TenantAssignmentAiPromptDto.cs`
- `src/Settings/SchoolCollab.Settings.Core/CQRS/AssignmentAiPrompts/Queries/GetTenantAssignmentAiPrompt/GetTenantAssignmentAiPrompt.cs`
- `src/Settings/SchoolCollab.Settings.Core/CQRS/AssignmentAiPrompts/Queries/GetTenantAssignmentAiPrompt/GetTenantAssignmentAiPromptHandler.cs`
- `src/Settings/SchoolCollab.Settings.Core/CQRS/AssignmentAiPrompts/Commands/UpsertTenantAssignmentAiPrompt/UpsertTenantAssignmentAiPrompt.cs`
- `src/Settings/SchoolCollab.Settings.Core/CQRS/AssignmentAiPrompts/Commands/UpsertTenantAssignmentAiPrompt/UpsertTenantAssignmentAiPromptHandler.cs`
- `src/Settings/SchoolCollab.Settings.Api/Endpoints/AssignmentAiPromptRoutes.cs`
- `src/Settings/SchoolCollab.Settings.Api/AssignmentAiPromptEndpoints.cs`
- `src/Settings/SchoolCollab.Settings.Core/Migrations/20260914145611_AddTenantAssignmentAiPrompt.cs` (+ `.Designer.cs`)
- `src/SchoolCollab.Admin.Shared/Services/AssignmentAiPromptApiClient.cs`
- `src/Settings/SchoolCollab.Settings.Application/Components/Pages/AssignmentAiPrompt.razor`
- `tests/SchoolCollab.Settings.Tests.Unit/TenantAssignmentAiPromptTests.cs`
- `tests/SchoolCollab.Admin.Tests.Unit/AssignmentAiPromptPageTests.cs`

**Modified (5):**

- `src/Settings/SchoolCollab.Settings.Core/Data/SettingsDbContext.cs` (DbSet + `ApplyConfiguration`)
- `src/Settings/SchoolCollab.Settings.Core/Migrations/SettingsDbContextModelSnapshot.cs` (+54 lines)
- `src/Settings/SchoolCollab.Settings.Api/Program.cs` (`MapAssignmentAiPromptEndpoints`)
- `src/Settings/SchoolCollab.Settings.Application/ModuleServices.cs` (client registration + `TenantPropagationDelegatingHandler`)
- `src/SchoolCollab.Admin/Components/Pages/Settings.razor` (fifth dashboard item)

## Migration

`20260914145611_AddTenantAssignmentAiPrompt` — additive: `Up` = `CreateTable("tenant_assignment_ai_prompts")` + unique tenant index (`is_deleted = false` filter); `Down` = `DropTable`. `SettingsDbContextModelSnapshot.cs` delta = +54 lines only (no other model drift). No existing migration touched.

## Build / test results (pass 1)

| Check | Result |
|---|---|
| `dotnet build SchoolCollab.sln` | **0 errors**, 28 warnings (all pre-existing; **zero warnings attributable to the new files** — verified by grep) |
| `tests/SchoolCollab.Settings.Tests.Unit` | **505 passed / 0 failed** (baseline 498 + 7 new) |
| `tests/SchoolCollab.Settings.Api.Tests.Unit` | 1 / 0 |
| `tests/SchoolCollab.Admin.Tests.Unit` | **545 passed / 0 failed** (baseline 541 + 4 new) |
| `tests/SchoolCollab.ArchitectureTests.Unit` | 20 / 0 |
| `tests/SchoolCollab.Core.Tests.Unit` | 79 / 0 |
| `tests/SchoolCollab.Assignments.Tests.Unit` | 510 / 0 |
| `tests/SchoolCollab.Assignments.Api.Tests.Unit` | 37 / 0 |
| `tests/SchoolCollab.Students.Tests.Unit` | 422 / 0 |
| `tests/SchoolCollab.Students.Api.Tests.Unit` | 1 / 0 |

## Deviations (reported, not silent)

1. **`Normalize` empty→null refinement (settings entity).** Whitespace-only or empty input stores `null` (restores the embedded default) rather than `""`. The plan's binding test name `SetPrompt_TrimsToNull` requires this; the ar-9 consent-text mirror persists `""` instead. One-line behavioural difference, covered by `SetPrompt_TrimsToNull`.
2. **Admin page tests use the `ScriptedHandler` HTTP-double, not MockHttp.** The plan's coverage list says "bUnit + MockHttp", but `SchoolCollab.Admin.Tests.Unit` has no MockHttp reference and the ar-9 consent-page precedent (`SignatureConsentTextPageTests.cs`) uses the scripted `HttpMessageHandler`. Mirrored the precedent (identical assertions: call shape, body, status mapping).
3. **Dashboard icon:** `Icons.Regular.Size24.Sparkle` — chosen from the repo's verified-in-use Size24 icon set (plan said "pick from the verified FluentIcons set").
4. **Round-doc prep edits by the parent** (mode line, base SHA, precondition checklist) — the parent owns the doc; no worker authored it.

## Pass 2 (step 3 — AI seam) files

**Created (1):**
- `src/AI/SchoolCollab.AI.Server/Services/TenantAssignmentAiPromptProvider.cs`

**Modified (9):**
- `src/AI/SchoolCollab.AI.Abstractions/AssignmentQuestionGenerationTypes.cs` (request gains `DifficultyEasyCount/Medium/Hard` + `ResourceTexts`)
- `src/AI/SchoolCollab.AI.Server/Services/AssignmentQuestionGenerationService.cs` (validation + org-prompt fetch + locked-ignore + new ctor dep)
- `src/AI/SchoolCollab.AI.Server/Services/AssignmentQuestionGenerationSystemPromptProvider.cs` (`BuildMessages(request, orgSystemPrompt)` + difficulty line + reference block)
- `src/AI/SchoolCollab.AI.Server/Program.cs` (`AddHttpClient<TenantAssignmentAiPromptProvider>` + `TenantForwardingDelegatingHandler`)
- `src/AI/SchoolCollab.AI.Server/Endpoints/AssignmentQuestionGenerationEndpoints.cs` (XML doc)
- `src/Assignments/SchoolCollab.Assignments.Application/ModuleServices.cs` (`propagateTenant: false` → `true`)
- `tests/SchoolCollab.Settings.Tests.Unit/AssignmentQuestionGenerationServiceTests.cs` (6 additive cases + 3 ctor fixes)
- `tests/SchoolCollab.Settings.Tests.Unit/AssignmentQuestionGenerationSystemPromptProviderTests.cs` (4 additive cases)
- `tests/SchoolCollab.Settings.Tests.Unit/TenantAssignmentAiPromptProviderTests.cs` (created, 4 cases)

## Pass-2 deviations

1. **Provider/resolver tests use a scripted `HttpMessageHandler`, not MockHttp.** `SchoolCollab.Settings.Tests.Unit` has no `RichardSzalay.MockHttp` reference and the round adds exactly ONE CPM package (HtmlAgilityPack), so no new package could be introduced — the ar-9 Admin-page `ScriptedHandler` pattern was mirrored instead (identical assertions).
2. `AssignmentQuestionGenerationService` gains a `TenantAssignmentAiPromptProvider` ctor dependency — DI injects it (registered in Program.cs); the service is registered by `AddAssignmentQuestionGeneration()` as a Singleton, auto-resolving the new param.
3. The org-prompt fetch is awaited inline in `GenerateAsync` (before `BuildMessages`), not fanned out with the model resolution — a single extra settings-api round-trip per generate call, fail-open to the embedded default on any failure.

## Pass 3 (step 4 — production) files + deviations + resume

**Created:** `src/Assignments/SchoolCollab.Assignments.Core/Domain/Exceptions/InvalidQuestionsDraftException.cs`; migration `20260914163358_AddAssignmentAiConfig` (+ Designer).

**Modified:** `Assignment.cs` (3 difficulty `int?` + `QuestionsDraftJson` + Stage/Discard/Confirm + Create/Update params); `ContractTypes.cs` (3 DTO records trailing difficulty); `Create/Update AssignmentCommand` + handlers (named-arg threading); `AssignmentRoutes.cs` POST/PUT; `DTOs/AssignmentSummary.cs` + `AssignmentRepository.ListAsync` + BOTH query handlers (intermediate-projection threading per decision (c)); `DuplicateAssignmentCommandHandler.cs` (difficulty clone, NOT the blob); `AssignmentEditFormModel.cs` (props + LoadFrom + ToCreateRequest).

**Deviations (reported):** (1) the Core `AssignmentSummary` DTO + `AssignmentRepository` were modified — NOT in the plan's expected-files list, but required by the plan's own "intermediate projection" note (decision (c)); (2) migration `AddAssignmentAiConfig` is additive (4 AddColumn in Up; Down drops) — difficulty columns + `questions_draft_json` nullable text.

**Verified:** full solution build 0 errors; `Assignments.Tests.Unit` 510/0 (no regression, `MigrationGuard` green WITH the new migration).

---
## Pass-5 (step 5 backend) — DONE

**Created:** `CQRS/Assignments/Commands/QuestionsDraft/` (Stage/Confirm/Discard command + handler), `CQRS/Assignments/Queries/QuestionsDraft/` (GetQuestionsDraftQuery + handler), `Core/Services/IAiPromptPolicyResolver.cs`, `Api/Services/AiPromptPolicyResolver.cs`.

**Modified:** `Assignments.Api/Program.cs` (scoped `IAiPromptPolicyResolver` beside `SignatureDefaultResolver`, `settings-api` reused); `AssignmentRoutes.cs` (QuestionsDraft usings + `StageQuestionsDraftBody` + 4 draft routes: `GET/PUT/{id}/questions-draft`, `POST …/confirm`→200+updated summary, `DELETE …/questions-draft`; NotFound→404, `InvalidQuestionsDraftException`→409, `AssignmentQuestionValidationException`→400; + `/ai-prompt-policy` always-200); `AssignmentsApiClient.cs` (`GetAiPromptPolicyAsync`, `Get/Stage/Confirm/DiscardQuestionsDraftAsync` + 4 private wire records).

**Deviations:** (1) client 409 via `EnsureSuccessStatusCode` (Application does NOT reference Core → cannot use `InvalidQuestionsDraftException`); wizard catches `HttpRequestException` next. (2) Step-5 tests deferred to freeze: `Stage/Confirm/DiscardQuestionsDraftCommandHandlerTests`, `AiPromptPolicyResolverTests`, draft-route tests.

**Verified:** full solution build 0 errors; `Assignments.Tests.Unit` 526/0; `Assignments.Api.Tests.Unit` 37/0.

## Resume (step 6 lead — wizard surfaces)

1. (f) wizard lock+difficulty: `Create.razor` `_aiPromptLocked` + `LoadAiPromptPolicyAsync()`; `QuestionGenerationSection.razor` `[Parameter] bool PromptLocked` + `Disabled`+tooltip on override + 3 `FluentNumberField<int?>` difficulty (Min 0 / Max 30, W2) threaded into `QuestionGenerationRequest` and persisted to `Model.DifficultyEasy/Medium/HardCount`.
2. (g) URL ingestion: CPM `HtmlAgilityPack` 1.11.72 in `Directory.Packages.props` + `Assignments.Application.csproj` `<PackageReference Include="HtmlAgilityPack" />` (no Version); `IUrlTextExtractor` + `UrlTextExtractor` (scheme http/https, 512KB body cap, strip script/style, truncate 20k chars); `AddHttpClient("url-fetcher", 5s)` in ModuleServices (NO tenant propagation); `AssignmentEditFormModel` `ResourceUrls` + `ResourceUrlRow` + `Add/RemoveResourceUrl` + map to `NewResourceDto` in `ToCreateRequest`; `ResourcesSection` URL block; `QuestionGenerationSection.OnGenerateAsync` extract ≤3 (fail-open + warning bar).
3. Build + `Assignments.Tests.Unit` after each.

## Pass-6 (step 6 wizard surfaces) — DONE

**Created:** `Services/IUrlTextExtractor.cs`, `Services/UrlTextExtractor.cs`, `Components/Pages/Assignments/ResourceUrlRow.cs`.

**Modified:** `Directory.Packages.props` (HtmlAgilityPack 1.11.72), `Assignments.Application.csproj` (PackageReference, no Version), `ModuleServices.cs` (AddHttpClient url-fetcher 5s + AddTransient IUrlTextExtractor), `Create.razor` (AI-prompt lock load), `QuestionGenerationSection.razor` (PromptLocked + 3 difficulty fields + URL extraction ≤3 + warning), `AssignmentEditFormModel.cs` (ResourceUrls + Add/RemoveResourceUrl + ToCreateRequest→Resources), `ResourcesSection.razor` + `.css` (URL block). Test-harness fixes: `QuestionGenerationSectionBunitTests` (IUrlTextExtractor DI fake), `AssignmentCreateBunitTests` (ai-prompt-policy mock 200).

**Verified:** `dotnet build SchoolCollab.sln` 0 errors; `Assignments.Tests.Unit` **526/0** (the 7 bUnit harness gaps fixed). NOTE: `.ar-10` test runner is Microsoft.Testing.Platform — do NOT pass `--nologo`/`--blame-*`/`--no-restore` (testhost rejects them → "Zero tests ran"); use plain `dotnet test <proj> -v q`.

**Deviations:** step-6 new tests deferred to freeze: `UrlTextExtractorTests` (4), wizard/URL-block bUnit additions, difficulty-field tests.

## Resume (step 7 lead — draft UI)

`QuestionsDraftSection.razor` (self-contained local state, [Parameters] AssignmentId/GateEnabled/PromptLocked/OnConfirmed; count 1–30, 3 type checkboxes, 3 difficulty, optional prompt disabled-when-locked; Generate→IAssignmentQuestionGenerator→GeneratedQuestionMapper.ToNewQuestionDto→Api.StageQuestionsDraftAsync→reload preview; Confirm→Api.ConfirmQuestionsDraftAsync→OnConfirmed; Discard→Api.DiscardQuestionsDraftAsync→clear; init loads existing draft via Api.GetQuestionsDraftAsync; read-only preview ul w/ @key + FluentBadge; Draft-only). `Edit.razor` renders it when Draft + GateEnabled + PromptLocked via Api.GetAiPromptPolicyAsync(). Build + `Assignments.Tests.Unit` after each.

## Pass-7 (step 7 draft UI) — DONE

**Created:** `Helpers/GeneratedQuestionMapper.cs`, `Components/Pages/Assignments/QuestionsDraftSection.razor` (+ minimal `.razor.css`).

**Modified:** `Edit.razor` (`@using ...Helpers`, `_aiPromptLocked` + `LoadAiPromptPolicyAsync()` fail-open, renders `<QuestionsDraftSection>` when `_item.Status==Draft` with `GateEnabled=QuestionGenerationGate.IsEnabled(...)`, `OnDraftConfirmedAsync` reloads the summary).

**Deviations:** the section takes `TopicId`/`TopicName`/`GradeLevelId` params in addition to the plan's 4 listed params — generation needs the assignment's topic/grade context (Edit passes `_item.TopicId`/`TopicName`/`GradeLevelId`).

**Verified:** full solution build 0 errors; `Assignments.Tests.Unit` 526/0 (no regression).

## Step 8 (freeze) lead — next dispatch

1. Write the deferred tests: step 5 — `Stage/Confirm/DiscardQuestionsDraftCommandHandlerTests` (Assignments.Tests.Unit; mirror the in-memory EF BuildScope harness from `CreateAssignmentCommandHandlerQuestionsTests`/`DuplicateAssignmentCommandHandlerTests`), `GetQuestionsDraftQueryHandlerTests`, `AiPromptPolicyResolverTests` (Assignments.Api.Tests.Unit, 204⇒false / 200⇒dto / network⇒false; mirror `SignatureDefaultResolver`/existing resolver tests), draft-route tests; step 6 — `UrlTextExtractorTests` (reject non-http / strip script-style / truncate cap / fail-open), wizard + URL-block + difficulty bUnit additions; step 7 — `QuestionsDraftSectionBunitTests` (generate+stage loads preview, confirm invokes OnConfirmed + clears, discard clears, init loads existing draft, gate-disabled no call).
2. `dotnet build SchoolCollab.sln` → 0 errors; run the FULL matrix (`dotnet test` across all test projects: Assignments, Assignments.Api, Settings, Settings.Api, Admin, Students, Core, plus ArchitectureTests — ensure MigrationGuard green).
3. Freeze: `git add -N -- src tests`; write final `.ar-10-worker-report.md`; produce `documents/rounds/diffs-ar-10-ai-extensions.patch` (full diff) for the reviewer; report to the parent for accept + UI-tester handover.

REMINDER: this `dotnet test` runner is Microsoft.Testing.Platform — do NOT pass `--nologo`/`--blame-*`/`--no-restore` (testhost rejects them, "Zero tests ran"). Use plain `dotnet test <proj> -v q`.

## Pass-8 (freeze) — DONE

**Tests written + green:** `Stage/Confirm/DiscardQuestionsDraftCommandHandlerTests` (4+4+2), `GetQuestionsDraftQueryHandlerTests` (3), `UrlTextExtractorTests` (6), `AiPromptPolicyResolverTests` (Api, 4). (Step-4 tests landed earlier: `AssignmentDifficultyMixTests` 5, `AssignmentQuestionsDraftTests` 9.) Confirm handler unit-tested against a mocked `IAssignmentRepository` — the repo's "capturing fake repository" precedent — because EF InMemory has the Same-Context Load→Replace-Owned-Children→SaveChanges concurrency quirk.

**Verification:** `Assignments.Tests.Unit` **545/0**, `Assignments.Api.Tests.Unit` **41/0**, `Settings.Tests.Unit` **519/0** (baseline), Core/Students/Students.Api/Settings.Api/Admin all pass, **`ArchitectureTests.Unit` pass → MigrationGuardTests green** (with `AddAssignmentAiConfig`). Full matrix (2200): the only failures are **24 in `Settings.Tests.Integration` — all Docker-environmental** ("Failed to connect to Docker endpoint `npipe://./pipe/docker_engine`", incl. 3 OpenRouter-gated `ChatAsync`) — NOT ar-10.

**Freeze:** `git add -N -- src tests` (intent-only, no index content); `documents/rounds/diffs-ar-10-ai-extensions.patch` emitted (7260 lines / 330 KB, 44 new + 39 modified). No commits.

**Deviations recorded end-to-end:** (1) `AssignmentSummary` (Core DTO) + `AssignmentRepository` threaded for the list/detail difficulty round-trip (plan's "intermediate projection" note); (2) no new typed exception beyond `InvalidQuestionsDraftException`; (3) `QuestionsDraftSection` takes TopicId/TopicName/GradeLevelId params beyond the 4 listed (generation context); (4) plan's `QuestionsDraftSection` UI bUnit NOT authored — the plan's required binding-test list does not include it; its intended behaviors are recorded for the UI-tester handover; (5) settings resolver ApiClient 409 via EnsureSuccessStatusCode (Application has no Core ref).

**Handover:** reviewer should statically verify the patch (build authority), then UI-tester bug-hunts the wizard difficulty/lock fields, ResourcesSection URL block, and the QuestionsDraftSection Draft-only regenerate/confirm/discard flow.

## Pass-R1 (rework) — DONE

Rework pass R1 closes the review P1s (all 9 items). Full solution build **0 errors**.

**Production fixes:**
- **R7** `QuestionGenerationSection.razor`: difficulty fields now bind directly to `Model.DifficultyEasyCount/Medium/HardCount` (`@bind-Value`), so editing + Save without Generate no longer drops the mix (the decision-(c) silent-reset class). Removed the redundant local `_difficulty*` fields + the post-generate write-back.
- **R8** `TenantAssignmentAiPromptProvider.cs` + `AiPromptPolicyResolver.cs`: added timeout fail-open — `OperationCanceledException when (!ct.IsCancellationRequested)` → log + return null/false; genuine caller cancellation still propagates.
- **R9** `AssignmentAiPrompt.razor`: added the Cancel-via-back link (`Nav.NavigateTo("/settings")`), mirroring the ConfigFlags Cancel convention.

**Tests added (plan binding list):**
- **R1** `GeneratedQuestionMapperTests.cs` (3): Maps_MultipleChoiceWithOptions, Maps_TrueFalse_Canonicalized, Maps_ShortAnswerWithModelAnswer.
- **R2** `QuestionsDraftSectionBunitTests.cs` (5): Generate_Then_Stages_Draft, Confirm_CallsClient_And_RaisesChanged, Discard_ClearsPreview, PromptLocked_DisablesGuidance, Renders_Preload_ExistingDraft (distinct @key via distinct question text).
- **R3** `AssignmentDraftRoutesTests.cs` (4): GetDraft_204WhenNone, PutDraft_Stages, ConfirmDraft_200And409Paths, DeleteDraft_204 (minimal TestServer harness, mirrors SignOffRoutesTests).
- **R4** `ResourcesSectionBunitTests.cs` (additive URL): AddUrl_AddsRow_AndRemovesAndRejectsDuplicate, AddUrl_Then_ToCreateRequest_MapsUrlResourceRows.
- **R5** `AssignmentFormModelMappingsTests.cs` (additive): LoadFrom_CarriesDifficultyCounts, ToCreateRequest_ThreadsDifficultyCounts, ToCreateRequest_ResourceUrls_MapToUrlResourceRows, ToCreateRequest_NoResourceUrls_NullResources, AddResourceUrl_TrimsAndAdds_RejectsDuplicateAndBlank, RemoveResourceUrlAt_InRange_Removes_OutOfRange_NoOp.
- **R6** `QuestionGenerationSectionBunitTests.cs` (additive 5): Difficulty_Fields_ThreadIntoRequest, PromptLocked_DisablesTextArea, ResourceUrls_PassExtractedTexts, ResourceUrl_Failure_FailsOpenWithWarning, MoreThanThreeUrls_WarnsAndCaps.

**Tests run (MTP, all 8 projects, 0 failures):**
Settings.Tests.Unit 519/0; Settings.Api.Tests.Unit 1/0; Students.Tests.Unit 422/0; Students.Api.Tests.Unit 1/0; Admin.Tests.Unit 545/0; Assignments.Tests.Unit 566/0 (was 545 → +21 rework); Assignments.Api.Tests.Unit 45/0 (was 41 → +4); ArchitectureTests.Unit 20/0 (MigrationGuardTests green).

Deviation from plan: none. R2 preload GET returns `questions` envelope; R4 drives add via the rendered FluentTextField (ValueChanged) per the repo bUnit convention.
