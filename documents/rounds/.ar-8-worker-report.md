# Round ar-8-signature-defaults — Worker Report

## Status

Implementation COMPLETE via two worker passes (both hit the 30-min cap) + parent
authoritative reconciliation:
- **Pass 1** (deepseek-v4-flash): landed Steps 1–6 + most of Step 7 production
  code and 6 test files, timed out before the worker report.
- **Pass 2** (kimi-k2.7-code escalation): completed Step 7 (`Edit.razor`) +
  Step 8 UI tests, timed out before the worker report.
- **Parent authoritative pass** fixed the remaining test-code defects the passes
  left (build errors in `AssignmentCreateBunitTests.cs`, missing DI registration
  in the Admin test harness, a MockHttp URL mismatch, over-strict log assertion,
  and two test-shape deviations from the plan) — see Deviations.

## Changed files (64 total: 53 production + 11 test)

All 61 plan-expected files present plus 3: the Assignments API test csproj entry
(required for the MockHttp `SignatureDefaultResolverTests`), and 2 lines the
escalation worker added to existing test files beyond the plan's stated
modifications. One-for-one vs plan:
- Settings: TenantAssignmentPolicy entity/config/DTO, Get/Upsert CQRS,
  migration `20260910124751_AddTenantAssignmentPolicy` (cs+Designer+snapshot),
  AssignmentPolicyRoutes/Endpoints, Program mapping, ModuleServices
- Students: GradeAssignmentPolicy entity/config/DTO, Get/Upsert CQRS,
  migration `20260910125042_AddGradeAssignmentPolicy` (cs+Designer+snapshot),
  GradeLevelRoutes GET/PUT, StudentsApiClient methods, StudentsDbContext,
  GradeSignaturePolicyEditor.razor, GradeLevels/Detail.razor card
- Admin.Shared: AssignmentPolicyApiClient.cs
- Assignments: ISignatureDefaultResolver, SignatureDefaultResolver, migration
  `20260910125859_AddAssignmentRequiresSignature` (cs+Designer+snapshot),
  Assignment.cs RequiresSignature + create/update/duplicate threading,
  ContractTypes (3 records), AssignmentRoutes + Program, AssignmentsApiClient
  GetSignatureDefaultAsync, Create.razor checkbox + pre-fill, Edit.razor
  checkbox + round-trip, AssignmentEditFormModel
- Tests: TenantAssignmentPolicyTests/HandlerTests, GradeAssignmentPolicyTests/
  HandlerTests, SignatureDefaultsTests, SignatureDefaultResolverTests,
  GradeSignaturePolicyEditorTests (new), GradeLevelDetailPageTests (+1),
  AssignmentCreateBunitTests (+4), AssignmentFormModelMappingsTests (+2)

## Build

`dotnet build SchoolCollab.sln -c Debug`: **0 errors** (parent authoritative).

## Tests

| Project | Result |
|---|---|
| Settings.Tests.Unit | 491 passed, 0 failed |
| Settings.Api.Tests.Unit | 1 passed, 0 failed |
| Students.Tests.Unit | 422 passed, 0 failed |
| Students.Api.Tests.Unit | 1 passed, 0 failed |
| Admin.Tests.Unit | 537 passed, 0 failed |
| Assignments.Tests.Unit | 448 passed, 0 failed |
| Assignments.Api.Tests.Unit | 20 passed, 0 failed |
| ArchitectureTests.Unit | 20 passed, 0 failed |

## Deviations from plan

1. `AssignmentCreateBunitTests` new build errors (StateHasChanged access,
   MockHttp GetMatchCount arity) — parent-fixed to bUnit `cut.Render()` and
   `_mockHttp.GetMatchCount(req)`.
2. `SetupActivityGroups` mock URL used `/students/activity-groups*` but the
   client calls `/activity-groups*` — parent-fixed.
3. `Create_GradeSelected_PreFillsCheckboxFromResolvedDefault` over-strict
   `_createLogs` assert failed on benign Debug/Info logs — parent narrowed to
   Error/Warning; also added the missing no-grade signature-default mock and
   grade-matcher-first registration (MockHttp string matchers ignore query
   strings).
4. `Detail_RendersSignaturePolicyCard` used rendered markup; plan specifies the
   source-based precedent (ReadDetailSource) — parent aligned it.
5. `Editor_LoadFails_ShowsError` asserted a friendly string the component does
   not render (`_error = ex.Message`, the GradeNotificationPolicyEditor
   precedent) — parent changed the assertion to check the Error message bar.
6. Admin test harness missing `AssignmentPolicyApiClient` DI registration
   (broke all Detail renders) — parent added it.
7. Win/LF warnings on new files are cosmetic (git core.autocrlf).

## Residuals (unchanged from round doc)

- Approver/attribution `Guid.Empty` placeholders (D-6 identity round).
- ar-1 EF owned-children verification still open.
- The 3 extra changed lines (Assignments Api.Tests csproj + 2 test-file line
  additions beyond the plan's stated modification matrices) are additive and
  reviewer-verifiable in the patch.
