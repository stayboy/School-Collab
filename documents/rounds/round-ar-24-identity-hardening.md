# Round ar-24 — Identity hardening (ar-20/21 residual set, spec §6 Security)

- **Status: IN PROGRESS — Tier 3 lean, started 2026-09-21.** Provider: pi (`ollama`). Models: orchestrator/parent `ollama-cloud/deepseek-v4.1-flash` (session), scope review `ollama/glm-5.3:cloud`, plan review **reviewer model** `ollama-cloud/deepseek-v4.1-flash` (lean rule), worker `ollama/deepseek-v4-flash:0731-cloud`, diff review `ollama-cloud/deepseek-v4.1-flash`. **UI tester: none** — the round's only UI touch is a placeholder-id correction with no visual diff (the deterministic trigger does not fire; ar-21 precedent). Branch will be cut at `a588a242` (current `main`).
- **Inputs:** recon `documents/rounds/.ar-24-identity-recon.md` (run `f818139b`, 2026-09-21, read-only, file:line-evidenced).
- **Scope review (2026-09-21, run `904f4e86`): REWORK with 2 P1 + 6 P2 + 3 nits — ALL LANDED.** The verbatim block is preserved in `## Scope review` below; the only open decision it left (P1-1's receiving-side opt-in) was decided by the parent: **include the Bearer opt-in on the targeted students-api/settings-api groups** (that is what makes the flag flip survivable and closes ar-21's accepted 409 `UnknownTeacher` deviation), plus a new row 7 hardening the directory fail-open. The `## Plan` (authored next) is the plan-review and worker contract.

## Scope v2 (post-scope-review; the plan's input)

### Findings (from the recon, with verdicts)

| # | Finding | Verdict |
|---|---|---|
| F1 | Cross-tenant `teacher_id` validation — principal-wins exists but **no tenant cross-check**; body ids honored in TestAuth; directory check is existence-only | **STILL TRUE** |
| F2 | `DisableOIDCAuth` flag-key split-brain | **LARGELY FIXED** — all five hosts read the canonical `FeatureFlags:FEATURE:DisableOIDCAuth`; the AppHost deliberately does not fan it out; `AuthWiringTests` pins the key |
| F3 | Bearer-only surfaces after a flag flip — Families/Admin clients attach tenant/deeplink headers but **no bearer** anywhere; assignment endpoint groups opt into Bearer | **STILL TRUE** |
| F4 | Service-to-service token forwarding — every cross-context hop forwards tenant only; no client-credentials; no token handler | **STILL TRUE** |
| F5 | Route-level authorization tests — flag=true shape tests exist; **flag=false 401/403 tests missing** (testing.md requires them) | **PARTIALLY TRUE** |
| F6 | Dev `staff_number` NULL in the seeded teacher | **CONFIRMED** |
| F7 | `TestAuthShape_…` tests | **REAL BUT NARROW** — claim-shape equivalence only; not vacuous, but covers none of the hardening surface |
| F8 | Submission-review identity threading — review takes body/route teacher ids (`AssignmentRoutes.cs:794,814-820,763`); `Detail.razor` passes `Guid.Empty` placeholders (`:735,764`) | **STILL TRUE** |

### In scope

| Row | Finding | Work |
|---|---|---|
| 1 | F1 + F8 | **Claim-wins + tenant cross-check**, enumerated exactly (P2-3): `ReviewSubmission`, **`ReviewAssignment`**, **`Approve`**, **`Override`** (all four cross-checked) **+ `GetSubmissionsForReview`** (review-queue query). Principal wins; wire-field fallback **only** in TestAuth/dev (ar-20's P1-6 pattern, `MissingTeacherPrincipalException` in real-auth). **Cross-check pinned (P2-4) to `ICurrentUser.CurrentTenant` vs `assignment.TenantId`** — the home-tenant lookup is out (would need a `TeacherDto.TenantId` contract change, which the gates forbid); the EF global tenant filter exists, so AC1's discriminating tests are built against fakes/`SuppressTenantGuard`, not a filtered DbContext. The review-queue route maps the typed rejection to a definitive **403, never a 500** (ar-20 precedent). |
| 2 | F8 | **`Detail.razor` placeholder ids** (`Guid.Empty`) replaced with the real acting-teacher id — resolved via the **component's auth-state path (circuit principal / `teacher_id` claim)**, NOT `ICurrentUser` (P2-5: `IHttpContextAccessor` is null in the Admin interactive circuit). Mechanism choice belongs to the plan. Behaviour-only, no visual diff. |
| 3 | F3 + F4 | **Bearer forwarding + receiving-side opt-in** (P1-1, P1-2): (a) one shared bearer-forwarding `DelegatingHandler` (keyed on the auth-mode flag: attaches the caller's bearer in real-auth; no-op in dev bypass), attached to the **interactive** cross-context named clients (Families, Admin, and the API-to-API hops); **worker/background clients are carved out** — no caller bearer exists and client-credentials is barred by the no-new-secrets gate, so the handler is not attached there and post-flip worker→settings/students 401s are a **named residual**; (b) **the receiving side opts in**: the students-api/settings-api groups these hops target get `AddAuthenticationSchemes(BearerScheme)` mirroring `AssignmentEndpoints.cs:19-25` (real-auth `DefaultScheme` is Cookie, so a forwarded bearer is otherwise rejected); (c) migrator→settings-api is **settled: no HTTP client exists** — excluded; (d) registration lands in shared `Add{Layer}()` extensions (nit-11), never inline per-host `Program.cs`. |
| 4 | F5 | **Route-level flag=false tests**: 401 without token per affected endpoint group (P2-7 — foreign-actor rejection is AC1's handler-level tests; a WebApplicationFactory test cannot mint an authority-valid foreign-actor bearer without swapping the auth handler), per `.github/copilot/rules/testing.md`. |
| 5 | F6 | **Dev seeder sets `staff_number`** (a clearly dev-only value) + test. |
| 6 | docs | **Both** registry files (P2-8): `documents/solution/assignment-request-go-forward-breakdown.md` (ar-21 merge SHA `91e7b250`, ar-22 entry `a588a242`, ar-23 entry `3a54ea91`, ar-21 "Residual: AC9" → closed, stale lines ~382–385 "awaiting owner decisions D1–D3") **and** `documents/solution/assignment-request-implementation-details.md` §3 round log. Plus `documents/configuration.md` (auth-mode/bearer-forwarding + receiving-side opt-in notes). Dead citation note (nit-10): `src/Config/…` is an empty directory — do not chase it. |
| 7 | F1 (review-found) | **Directory fail-open hardening**: `TeacherDirectoryHttpClient.ExistsAsync` treats any 2xx as "exists" and `HttpClient` auto-follows the OIDC 302 challenge to the Keycloak login page (200) under real-auth, so bogus teachers pass. Fix: `AllowAutoRedirect = false` for that client + strict 2xx-only success (anything else = not-exists), + discriminating test. |

### Out of scope (explicit)

- OIDC for the `portals` app (prefab plan Phase 3 — separate round, gated on the ward-MVP re-decide).
- `Aspire.Hosting.Keycloak` adoption + production realm-import story (Phase 5 WS-G).
- F2 re-touch (already fixed; only documented).
- Any new feature-flag semantics beyond attaching to the existing canonical key.
-students-api/students-worker handler-side identity threading beyond what rows 1/3 require (recon found no finding there).
- Production identity provider config (realm, tokens, lifetimes) — dev-IdP only.
- No contract-shape changes, no migrations, no CPM changes, no new secrets.

### ACs (draft — plan will finalize; acceptance bar)

1. **AC1 claim-wins + tenant cross-check (P2-3, P2-4)**: the **four** handlers (`ReviewSubmission`, `ReviewAssignment`, `Approve`, `Override`) + the review-queue query resolve identity principal-first with the ar-20 auth-mode-keyed fallback; a foreign-tenant teacher cannot act (typed exception); the queue route maps the typed rejection to **403, never a 500**. Discriminating tests (built against fakes/`SuppressTenantGuard`, not the filtered DbContext): same-tenant allowed, foreign-tenant rejected, TestAuth wire-field still works, queue route returns 403 not 500.
2. **AC2 bearer forwarding + receiving side (P1-1, P1-2)**: the shared handler exists, is attached to the **interactive** cross-context named clients, attaches the bearer only in real-auth mode (tests: header present flag=false; absent flag=true); **worker clients are excluded by design** (test wording states the carve-out); the targeted students-api/settings-api groups opt into the Bearer scheme (verified in the flag=false route tests).
3. **AC3 route tests (P2-7)**: flag=false **401 with no token** per affected endpoint group, passing.
4. **AC4 seeder**: `staff_number` seeded in dev; test asserts.
5. **AC5 hygiene (nit-9)**: full build 0 errors; **whole matrix ≥ 2,391, 0 failures**; ArchitectureTests 29/0; Core 92/0; no gate broken (no contract shape, no migration, CPM untouched, flags centralized); both registry files + configuration docs updated.

### Risks / open questions for the scope review

- R1: Does the bearer-forwarding handler risk breaking the **dev-bypass** path or the bUnit/MockHttp suites (ar-13's matcher lesson)? Mitigation: handler no-ops in dev bypass; suites re-run.
- R2: **Replaced (P2-6)** — `CrossModuleWiringTests` is base-address→`WithReference` wiring only and cannot enforce handler attachment. Row 3's client list is verified during planning from the named-client registrations themselves, and enforcement comes from a **new/extended guard** (scan named-client registrations and assert the shared handler registration) or an explicit statement in the plan that no automated guard covers it. Migrator exclusion is settled (no client exists). **Named residual:** post-flip worker→settings/students 401s (no caller token; client-credentials barred by the no-new-secrets gate) — a later round with a credentials decision.
- R3: Choosing the foreign-tenant rejection outcome (403 vs 409 vs typed 4xx) touches API contract behaviour — must stay within the existing contract conventions (no shape change); decide in the plan, not the code.
- R4: `review-queue` takes `teacherId` as a **route param** (`AssignmentRoutes.cs:814-820`) — principal-wins changes its observable contract only in real-auth mode; confirm no consumer depends on the route param in real-auth.
- R5: TestAuth mode must keep working for the whole existing bUnit/suite corpus — the ar-20 keyed-fallback pattern is the template; any deviation is a P1.

## Scope review (run `904f4e86`, 2026-09-21 — REWORK 2 P1 + 6 P2 + 3 nits, ALL LANDED)

> Parent landing summary: P1-1 → row 3(b) receiving-side opt-in + row 7 directory fail-open; P1-2 → row 3 worker carve-out + named residual in R2; P2-3 → row 1 enumeration + AC1 403-never-500; P2-4 → cross-check pinned to `ICurrentUser.CurrentTenant` vs `assignment.TenantId` + test-design note; P2-5 → row 2 circuit-principal mechanism; P2-6 → R2 rewritten (new/extended guard, migrator settled); P2-7 → row 4/AC3 reworded to 401-only; P2-8 → row 6 names both registry files; nit-9 → AC5 whole-matrix wording; nit-10 → dead citation note in row 6; nit-11 → row 3(d) registration in `Add{Layer}()` extensions.

**Verdict: REWORK with 2 P1** (both are row/AC text fixes; all recon claims spot-checked were still true — no fabricated findings, evidence below).

### P1

1. **Row 3/AC2 miss the receiving side — a forwarded bearer is rejected by the target groups, so handler-only doesn't fix the flag flip.** Real-auth `DefaultScheme` is Cookie (`src/SchoolCollab.Core/Auth/AuthTenancyExtensions.cs:79-81`); the assignments groups opt into Bearer explicitly for exactly this reason (`src/Assignments/SchoolCollab.Assignments.Api/AssignmentEndpoints.cs:19-25` + comment). Students-api and settings-api groups use plain conditional `RequireAuthorization()` with no `AddAuthenticationSchemes(BearerScheme)` (`src/Students/SchoolCollab.Students.Api/StudentEndpoints.cs:13-15,47-49,55-57`; `src/Settings/SchoolCollab.Settings.Api/CodedValueEndpoints.cs:20-22` and siblings). Consequences: (a) the round's motivating deviation (ar-21's accepted 409 `UnknownTeacher`, `round-ar-21…md:38`) stays broken; (b) latent fail-open — `TeacherDirectoryHttpClient.ExistsAsync` treats any 2xx as "exists" (`TeacherDirectoryHttpClient.cs:14-27`), and HttpClient auto-follows the OIDC 302 challenge to the Keycloak login page (200) under real-auth, so bogus teachers pass. **Fix:** add the receiving-side opt-in (Bearer scheme on the students-api/settings-api groups the hops target, mirroring AssignmentEndpoints) to row 3's work and to AC2 — or explicitly bound row 3 to "token attached" and record the receiving side + directory fail-open as named residuals.

2. **Row 3/AC2 are unsatisfiable for background clients: no caller bearer exists, and client-credentials is barred by the round's own "no new secrets" gate.** The worker registers `AddHttpClient("settings-api")`/`AddHttpClient("students-api")` for background sweeps (`src/Assignments/SchoolCollab.Assignments.Worker/Program.cs:36-37`); there is no HttpContext/caller token to forward there, so AC2's "attached to **every** cross-context named client … header present flag=false" cannot pass for those clients. (Recon gap resolved: `src/SchoolCollab.MigrationService` has **no** HTTP client at all — exclude it as settled.) **Fix:** carve worker/background clients out of row 3/AC2 (handler not attached, or explicitly no-token no-op with a test wording to match), and record post-flip worker→settings/students 401s as a named later-round residual.

### P2

3. **Row 1/AC1 enumeration gap: the `ReviewAssignment` handler is dropped, and the queue route will 500 on the new rejection.** Recon §1 cites `AssignmentRoutes.cs:550`; `ReviewAssignmentCommandHandler.cs:26-29` already has ar-20 claim-wins but no tenant cross-check — yet row 1 lists only "ReviewSubmission, the approve/override path, and the review-queue query", and AC1's "three submission handlers" doesn't even match that list (approve is assignment-level). The queue route is a bare `MapGet` with no catch blocks (`AssignmentRoutes.cs:807-812`), so a valid bearer without a `teacher_id` claim will surface `MissingTeacherPrincipalException` as a 500 — violating the ar-20 "never a 500" precedent its sibling routes implement. **Fix:** enumerate exactly ReviewSubmission + ReviewAssignment + Approve + Override (cross-check all four) + `GetSubmissionsForReview`; add "queue route maps the typed rejection to a definitive 4xx (403), never a 500" to AC1.

4. **Row 1's cross-check data source must be pinned or it's either redundant or contract-breaking.** "The teacher's tenant" is only resolvable today as the claim/ambient tenant (`CurrentUser.cs`; `TenantClaimsTransformation`); the *home* tenant is not — `TeacherDto` has no `TenantId` (`src/Students/SchoolCollab.Students.Core/DTOs/TeacherDto.cs:5-13`) and `ITeacherDirectory` is existence-only, so a home-tenant lookup = a students-api contract-shape change the round's own gates forbid. Note also the EF global tenant filter exists (`src/SchoolCollab.Core/Data/EntityTypeConfigurationBase.cs:214-262` `ConfigureTenantQueryFilter`; sanctioned bypass in `TenancyServiceExtensions.cs`) — the recon's could-not-establish item — so claim-tenant-vs-assignment-tenant is defense-in-depth, and AC1's foreign-tenant test must be built against fakes/bypass, not a filtered DbContext. **Fix:** pin row 1 to `ICurrentUser.CurrentTenant` vs `assignment.TenantId`; record the filter posture for test design.

5. **Row 2's mechanism parenthetical is wrong for the Admin host: `ICurrentUser` is unreachable from the interactive circuit.** `CurrentUser` is `IHttpContextAccessor`-backed (`CurrentUser.cs: Principal => httpContextAccessor.HttpContext?.User`), and Detail.razor runs under the Admin host's `InteractiveServerRenderMode(prerender: false)` (`src/SchoolCollab.Admin/Components/App.razor:33`; `SchoolCollab.Admin.csproj:16` → Assignments.Application) — HttpContext is null in the circuit event handlers where the `Guid.Empty` placeholders fire (`Detail.razor:735,764`). **Fix:** reword row 2 to resolve the acting teacher via the component's auth-state path (circuit principal / `teacher_id` claim) captured for the interactive lifetime; mechanism choice belongs to the plan.

6. **R2 names the wrong backstop: `CrossModuleWiringTests` cannot enforce row 3's client list.** Its own contract is base-address→`WithReference` wiring only (`tests/SchoolCollab.Core.Tests.Unit/Architecture/CrossModuleWiringTests.cs:6-10`) — nothing about `DelegatingHandler` attachment. **Fix:** extend the guard (scan named-client registrations and assert the shared handler is registered) or drop the "enforcement backstop" claim; record the migrator exclusion as settled (no client exists).

7. **AC3's "foreign-actor rejection tests … for the affected groups" is over-specified at route level.** Real-auth `AddJwtBearer` validates against the Keycloak authority (`AuthTenancyExtensions.cs:107-125`); a WebApplicationFactory test cannot mint an authority-valid foreign-actor bearer without swapping the auth handler — and AC1 already owns the discriminating handler-level rejection test. **Fix:** reword AC3 to "flag=false route tests assert 401 with no token per affected endpoint group; foreign-actor rejection is AC1's handler-level tests".

8. **Row 6 omits the second registry file and names neither.** Resume-state item 8 (`.ar-24-resume-state.md:47`) scopes the fold-in to **both** `assignment-request-go-forward-breakdown.md` (stale lines ~382-385 "awaiting owner decisions D1–D3" — verified at breakdown:384; ar-22/ar-23 entries absent; ar-21's residual headline is indeed AC9, `round-ar-21…md:291-293`) **and** `assignment-request-implementation-details.md` §3 round log ("likewise"). **Fix:** name both files in row 6.

### Nit

9. AC5's "≥ previous 2,391+ baseline **for touched suites**" — 2,391 was the whole-matrix count at the ar-20 merge (breakdown:381 "matrix 2,391/0"); say "whole matrix ≥ 2,391, 0 failures".
10. Recon §2's Config citation is dead: `src/Config/SchoolCollab.Config.Api/` is an empty directory (no `DisableOIDCAuth` anywhere under `src/Config`). The scope's "all five hosts" claim is still accurate — just don't chase the citation in the plan.
11. Row 3's registration must land in shared `Add{Layer}()` extensions (precedent: `TenantPropagationDelegatingHandler` in Core/Auth + ModuleServices wiring), never inline per-host `Program.cs` — dotnet-best-practices.md Never-list ("❌ `services.Add*()` inline in feature Program.cs").

### Confirmed correct (spot-checked, no action)

- **F2 "LARGELY FIXED" is accurate** — all five hosts use the canonical `FeatureFlags:FEATURE:DisableOIDCAuth` (appsettings grep: Admin/Assignments/Students/Families/Settings), AppHost deliberately does not fan it out (only comments `AppHost/Program.cs:182-184,359`; only `RequireAssignmentApproval` is fanned at :283/:342), `AuthWiringTests.cs:33-36` pins the key. Document-only is the right handling.
- **Model/traceability line matches recorded policy** — models.md/SKILL.md:82: plan-review on full Tier 3 = `glm-5.3:cloud`, **lean = the reviewer model** (`deepseek-v4.1-flash`, owner override 2026-09-15); the status line records exactly that.
- Rows 1/2/4/5/6 are each backed by still-true findings: `ApproveAssignmentCommandHandler.cs:32-40` (claim-wins, no cross-check), `Detail.razor:735,764` placeholders, `WardRoutesTests.cs:55-61` TestAuth-only stubs + `testing.md:96` both-states mandate, `DevIdentitySeeder.cs:84-92` `staff_number` NULL, registry staleness verified. No scope creep; R1 (ar-13 MockHttp limitation is real, `round-ar-13…md:287`), R3 (403 precedent exists on sibling routes), R5 are sound; R4 is already answerable — no src consumer calls review-queue (only the out-of-scope prefab plan doc references it).

**Merge verdict: REWORK with 2 P1** — rework is text-only (rows 1–3, AC1–AC3, R2); no in-scope row must be dropped and no out-of-scope item needs to move in beyond the receiving-side opt-in decision in P1-1.

## Plan

**Goal.** Make the `DisableOIDCAuth=false` (real-auth) posture survivable and the acting-teacher identity server-authoritative: claim-wins + tenant cross-check on the whole submission-review surface, bearer forwarding with receiving-side Bearer opt-in on every hop the round touches, flag=false route tests, a dev seeder fix, and the directory fail-open closed — with **no contract-shape change, no migration, CPM untouched, flags centralized on the existing canonical `FeatureFlags:FEATURE:DisableOIDCAuth` key, no new secrets, and TestAuth/dev behaviour unchanged** (R5: wire-field fallback stays the TestAuth contract; `TestAuthHandler` stamps no `teacher_id` claim by default — `TestAuthHandler.cs:29-33,103` — so dev attributions keep working exactly as today). Zero backend contract-shape change; the `portals/` app, OIDC, and `Aspire.Hosting.Keycloak` stay out.

### Verified anchors (orchestrator-checked 2026-09-21 — supersedes recon/review line numbers where they differ)

| Anchor | Verified |
|---|---|
| Routes file | **`src/Assignments/SchoolCollab.Assignments.Api/Endpoints/AssignmentRoutes.cs`** (the scope review's shorter path is wrong) — review-queue `:807-812` (bare `Results.Ok`, no catch → 500 on a typed exception); review-submission `:794` + `UnauthorizedAccessException → Results.Problem(…, 403)` at **`:801-803`** (catch at `:801`, `Results.Problem` at `:803` — nit-1); override `:763`; `ReviewAssignmentCommand` `:550`; the **`MissingTeacherPrincipalException → 403` catch precedent exists at `:248, :445, :476, :559, :775`** |
| Handlers | `ApproveAssignmentCommandHandler.cs:32-40` (claim-wins keyed on auth mode — template); `ReviewAssignmentCommandHandler.cs:26-29` (claim-wins, no cross-check); **`ReviewSubmissionCommandHandler.cs` — NO claim-wins at all** (`command.TeacherId` raw at `:19, :27, :38`; ownership check `CreatedByTeacherId != command.TeacherId` → `UnauthorizedAccessException` at `:27-29`); `OverrideStudentSubmissionAttemptsCommandHandler.cs:37` (claim fallback); `GetSubmissionsForReview(Guid TeacherId)` record + handler passes through (`GetSubmissionsForReviewHandler.cs:14-15`) |
| `ReviewSubmissionGate` | takes **no** teacher id (`ReviewSubmissionGateCommand.cs` / handler — zero `TeacherId` references) → **excluded** from row 1, recorded here so the worker doesn't hunt for it |
| Identity source | `src/SchoolCollab.Core/Auth/ICurrentUser.cs` — `IsAuthenticated`, `Guid? TeacherId`, **`TenantContext CurrentTenant`**; impl `CurrentUser.cs` is `IHttpContextAccessor`-backed (`:16,19,25,40`) |
| Schemes | `AuthTenancyExtensions.cs`: `BearerScheme = "Bearer"` (:24), real-auth `DefaultScheme = Cookie` (:81); the Bearer opt-in pattern to mirror is **`AssignmentEndpoints.cs:15-25`** (verbatim verified) |
| Receiving-side gap | `StudentEndpoints.cs` has **five** conditional blocks — `:11-14`, `:36-39`, `:48-51`, `:55-58`, **`:65-68` (`/teachers` — the group the directory check actually calls, P1-2)** — all plain `RequireAuthorization()`; **eight** Settings.Api endpoint files plain `RequireAuthorization()` (`CodedValueEndpoints.cs:22`, `ConfigEndpoints.cs:25`, `EntityCodeRuleEndpoints.cs:19`, `AssignmentAiPromptEndpoints.cs:22`, `AssignmentPolicyEndpoints.cs:20`, `NotificationPolicyEndpoints.cs:20`, `SignatureConsentTextEndpoints.cs:21`, `TenantEndpoints.cs:20`) |
| Named clients (P1-1/P1-7 inventory, reviewer-verified) | **API-to-API hops (the ONLY attachments)**: `Assignments.Api/Program.cs:109` ("students-api"), `:116` ("settings-api"); `Students.Api/Program.cs:44, :62, :71`. **Worker carve-out**: `Assignments.Worker/Program.cs:36-37` (no caller token). **NOT attached — named residual (P1-7)**: Blazor-side interactive clients, whose registration sites are `Families/Services/FamiliesModuleServices.cs:17` (`AddCrossModuleHttpClient<FamiliesApiClient>("https+http://assignments-api", propagateTenant: true)` — a shared helper, NOT `AddHttpClient`), `Students.Application/ModuleServices.cs:22`, `Assignments.Application/ModuleServices.cs:33`, and the seven settings typed clients in `Settings.Application/ModuleServices.cs` (`:34-36, :47, :53/:60, :69, :78, :87, :96`); the Admin host registers none of its own (`Program.cs:47-50` calls the three `Add*Module()`s). No settings-api→students-api hop was verified (P2-2) — do not invent one. |
| Directory fail-open | `TeacherDirectoryHttpClient.cs:13-32` — `CreateClient("students-api")` → `GET teachers/{teacherId}` → `IsSuccessStatusCode` (strict 2xx already); the fail-open is purely the **auto-redirect** following the 302 challenge to a 200 login page |
| Seeder | `DevIdentitySeeder.cs` — the dev teacher INSERT's `staff_number` column is the 9th and receives `NULL` (VALUES row: `'Dev Teacher', NULL, NULL, NULL` = display_name, staff_user_id, **staff_number**, level) |
| UI placeholders | `Detail.razor` has **four** `Guid.Empty` sites — `:625` (`ApproveAsync`), `:652` (`RejectAsync`), `:735`, `:764` (scope/recon cited only the last two) |
| TestAuth claim posture | `TestAuthHandler.cs:29-33,103` — `teacher_id` stamped **only** when `Options.TeacherId` is non-empty; the dev default stamps **no** teacher claim (`tenant_id` always stamped, `:95`) |

### Implementation rows

**Row 1 — claim-wins + tenant cross-check (Scope rows 1; P2-3, P2-4).**
- NEW `src/Assignments/SchoolCollab.Assignments.Core/Domain/Exceptions/TeacherTenantMismatchException.cs` — typed domain exception per dotnet-best-practices (home: `Core/Domain/Exceptions`, alongside `MissingTeacherPrincipalException.cs`, verified present), XML doc, message naming the assignment id + both tenants.
- **`ReviewSubmissionCommandHandler.cs` — ADD the ar-20 claim-wins pattern** (it does not exist there today): resolve `var teacherId = currentUser.TeacherId ?? (await IsRealAuthAsync(ct) ? throw new MissingTeacherPrincipalException(nameof(ReviewSubmissionCommand)) : command.TeacherId);` (copy the `IsRealAuthAsync` private helper from `ReviewAssignmentCommandHandler.cs`), use the resolved id in the ownership check (`:27`) and the domain call (`:38`), then add the cross-check.
- **Cross-check in all four**, source pinned **per handler (P1-3)** — `assignment.TenantId` for `ReviewSubmission`/`ReviewAssignment`/`Approve` (each loads the assignment first); **`submission.TenantId` for `Override`** (its route resolves only the submission — `AssignmentRoutes.cs:751-778` — and the handler never loads an assignment): `if (currentUser.CurrentTenant.TenantId != <pinned entity>.TenantId) throw new TeacherTenantMismatchException(…)` — source pinned to `ICurrentUser.CurrentTenant` (P2-4). Placement AFTER the existing load, BEFORE the domain mutation.
- **Arity change (P1-4)**: `ReviewSubmissionCommandHandler` gains `ICurrentUser` + `IFeatureFlagService` — **`tests/SchoolCollab.Assignments.Tests.Unit/SubmissionEngineTests.cs` has three existing 4-arg constructions (`:242, :257, :299`) that must be updated** (they are in the expected-files table). The existing fakes' tenants already align (`FakeCurrentUser.TenantId == 1111…`), so the cross-check must NOT be "fixed" by altering existing test tenants.
- **`GetSubmissionsForReview`** — no handler change (its repository call is already tenant-filtered by the ambient context); the fix lives at the route (below).
- Route work in `Endpoints/AssignmentRoutes.cs`: the review-submission route gains `catch (TeacherTenantMismatchException ex) → Results.Problem(ex.Message, statusCode: 403)` next to `:800-802`; the review-queue route (`:807-812`) resolves the teacher id principal-first — add `[FromServices] ICurrentUser currentUser` and `var teacherId = currentUser.TeacherId ?? (await realAuth ? throw new MissingTeacherPrincipalException("GetSubmissionsForReview") : teacherIdParam)` — and wraps the call in try/catch mapping `MissingTeacherPrincipalException` + `TeacherTenantMismatchException` to **403, never a 500** (mirroring `:775`'s precedent). The other three routes already catch `MissingTeacherPrincipalException` — extend each with the new exception.
- Tests: new `tests/SchoolCollab.Assignments.Tests.Unit/SubmissionReviewIdentityTests.cs` — **handler-level unit tests against in-memory/fake repositories + a fake `ICurrentUser`** (never a filtered DbContext — the EF tenant filter is bypassed by construction, P2-4), with the **foreign-tenant case parameterized over ALL FOUR handlers (P2-3)**: (1) same-tenant claim → allowed; (2) foreign-tenant claim → `TeacherTenantMismatchException` (×4); (3) real-auth + no claim → `MissingTeacherPrincipalException`; (4) TestAuth + no claim → wire field honored (R5); (5) review-submission claim-wins actually overrides the body id (new behaviour there). Route-level: the 403-never-500 queue assertion via the row-4 harness.

**Row 2 — `Detail.razor` acting-teacher resolution (P2-5).**
- Mechanism (decided): inject `Microsoft.AspNetCore.Components.Authorization.AuthenticationStateProvider` into `Detail.razor`; `GetActingTeacherIdAsync()` reads the circuit principal's `teacher_id` claim → `Guid.TryParse`; **no claim → `Guid.Empty`** (dev/TestAuth default posture stamps none — `TestAuthHandler.cs:29-33,103` — so dev behaviour is unchanged, R5 satisfied; in real-auth the handler claim-wins anyway, so the resolved value is belt-and-braces). Never `IHttpContextAccessor` (null in the circuit).
- Apply at **all four** sites: `:625`, `:652`, `:735`, `:764` (the methods are already async). **Explicit non-goal (nit-2): this does NOT fix the dev/TestAuth review path** — with no `teacher_id` claim the component still sends `Guid.Empty` and `ReviewSubmissionCommandHandler.cs:27-29`'s ownership check still rejects it; the dev UI review path stays broken by design (R5).
- Tests: extend `tests/SchoolCollab.Assignments.Tests.Unit/AssignmentDetailBunitTests.cs` (bUnit, ScriptedHandler MockHttp per repo rules): a stub `AuthenticationStateProvider` **with** a `teacher_id` claim → the outgoing API request body carries the resolved id; **without** → `Guid.Empty` (discriminating both ways).

**Row 3 — bearer forwarding + receiving-side opt-in (P1-1, P1-2, nit-11).**
- NEW `src/SchoolCollab.Core/Auth/BearerForwardingDelegatingHandler.cs` (alongside `TenantPropagationDelegatingHandler.cs`): transient `DelegatingHandler(IFeatureFlagService, IHttpContextAccessor, ILogger<>)`; when `DisableOIDCAuth` is enabled → pass-through (dev bypass no-op, R1); else copy the **current request's** `Authorization` header verbatim (`IHttpContextAccessor.HttpContext.Request.Headers.Authorization`) when present; never throws when absent. **Transient** (the `ModuleServices.cs:32-34` comment documents why handlers must be transient).
- DI registration: `services.TryAddTransient<BearerForwardingDelegatingHandler>()` in the Core auth extension that already registers the auth stack (`AuthTenancyExtensions.AddAuthAndTenancy` — the `AuthWiringTests`-covered home). ** nit-11 compliance:** the handler class + its TryAddTransient live in the shared extension; the `.AddHttpMessageHandler<BearerForwardingDelegatingHandler>()` attachments land at the **existing** named-client registration sites (modifying pre-existing inline registrations, not adding new ones), and where an `Add{Layer}` extension owns the client (Settings.Application `ModuleServices.cs:35-39`) the attachment goes there.
- Attachments (P1-1/P1-7 — **bounded to the API-to-API hops**): `src/Assignments/SchoolCollab.Assignments.Api/Program.cs:109,116`; `src/Students/SchoolCollab.Students.Api/Program.cs:44, :62, :71` (their inbound requests carry the bearer, so forwarding is real). **Families/Admin and the Application-layer typed clients are NOT attached — named residual (P1-7)**: they run in interactive circuits where `IHttpContextAccessor` is null and the browser carries the OIDC **cookie, not a bearer** (real-auth `DefaultScheme = Cookie`, `SaveTokens = true`) — there is no header to copy; a circuit-safe token source is a larger separate change. Their post-flip calls rely on row 1's claim-wins (server-authoritative) and are covered by the residual below.
- **Carve-out:** `src/Assignments/SchoolCollab.Assignments.Worker/Program.cs:36-37` gets **no** handler (no caller token; client-credentials barred by the no-new-secrets gate). Named residuals: post-flip worker→settings/students 401s **and** the Blazor-side bearer gap above.
- Receiving-side opt-in, mirroring `AssignmentEndpoints.cs:15-25` (same comment shape): students-api — inline at **all five** conditional blocks in `StudentEndpoints.cs` (`:11-14`, `:36-39`, `:48-51`, `:55-58`, **`:65-68` `/teachers` — P1-2, the group the directory check calls**); settings-api — **inline per file, as built** (the planned `SettingsEndpointAuth.cs` helper was NOT built — allowed by the per-API decision and it keeps the guard uniform): the **8 `*Endpoints.cs` files** plus **`ConfigFlagRoutes.cs`** (opt-in at the `"flag_admin"` policy registration — role claim **kept**, not weakened) and **`ConfigResolveRoutes.cs`** = **10 files** (parent decision, pass 3). The Cookie default scheme is untouched; Admin/Families browser flows hit the Blazor hosts, not these APIs, so nothing regresses.
- Guard: NEW `tests/SchoolCollab.ArchitectureTests.Unit/BearerForwardingWiringArchitectureTests.cs` (repo-root walk-up precedent, `AppHostSettingsDbWiringArchitectureTests`): (1) the handler class exists in Core/Auth; (2) `AddHttpMessageHandler<BearerForwardingDelegatingHandler>` present in each enumerated **API-side** registration file (`Assignments.Api/Program.cs`, `Students.Api/Program.cs`) and **absent** in `Assignments.Worker/Program.cs` (the carve-out is pinned); (3) **per-group** (P1-2): one Bearer opt-in per conditional block — **5 in `StudentEndpoints.cs` + 10 Settings files** (as built) — not merely per-file; (4) `ConfigurePrimaryHttpMessageHandler` + `AllowAutoRedirect = false` present on both `Assignments.Api/Program.cs` clients (row 7's discriminating assertion, P1-6). Revert-probe: remove the `:109` attachment → guard fails; remove the `/teachers` opt-in → guard fails.

**Row 4 — flag=false route tests (P2-7).**
- `tests/SchoolCollab.Assignments.Api.Tests.Unit/RealAuthRouteTests.cs` (NEW, P1-5): the harness must register the **real auth stack** — call `builder.Services.AddAuthAndTenancy(builder.Configuration)` with an in-memory config carrying `FeatureFlags:FEATURE:DisableOIDCAuth=false` + a dummy `Auth:Keycloak:Authority` (mirroring `tests/SchoolCollab.Core.Tests.Unit/Auth/AuthWiringTests.cs:31-46`); `WardRoutesTests`'s current harness registers neither auth nor authorization, so with flag=false the Bearer policy would be unenforced — without `AddAuthAndTenancy` there is **no 401 to assert**. Rely on WebApplication's auto-added auth middleware; AddJwtBearer validates lazily — **no token sent → 401 before any authority contact**, so no Keycloak and no handler swap. Assert **401** (no token) on: the assignments group, the ward list (`/students/{id}/assignments`), review-queue, and approve.
- students-api + settings-api: the WAF harnesses **already exist** — `tests/SchoolCollab.Students.Tests.Integration/ApiFactory.cs` and `tests/SchoolCollab.Settings.Tests.Integration/ApiFactory.cs` — but **both hard-force the flag to true** (`UseSetting("FeatureFlags:FEATURE:DisableOIDCAuth", "true")` + `UseEnvironment("Testing")`, and their appsettings also say `"true"`) — so the override mechanism must be stated explicitly (P1-5): **process env var `FeatureFlags__FEATURE__DisableOIDCAuth=false`** on the factory (env outranks `UseSetting`/appsettings) or a derived factory appending a last-wins in-memory config source — the plan pins the env-var route. One 401-no-token test each, one representative protected route per host. If a factory boots Testcontainers, the 401 short-circuits before any DB use (existing suite infra, no new containers).

**Row 5 — dev `staff_number` (F6).**
- `src/SchoolCollab.MigrationService/Seeding/DevIdentitySeeder.cs`: replace the `staff_number` positional `NULL` — **the NULL in column 9, i.e. the third of the three consecutive positional NULLs after `'Dev Teacher'`** (nit-3 — do NOT touch `staff_user_id`, the second NULL) — with a clearly dev sentinel (`'DEV-0001'`); nothing else in the INSERT changes.
- Test: NEW `tests/SchoolCollab.ArchitectureTests.Unit/DevIdentitySeederStaffNumberArchitectureTests.cs` (source-scan precedent, `AppHostRealmImportArchitectureTests`) asserting the seeder's INSERT carries a non-NULL `staff_number` literal. No new test project.

**Row 6 — docs.** `documents/configuration.md` (auth-mode/bearer-forwarding/receiving-side opt-in + the named worker residual); `documents/solution/assignment-request-go-forward-breakdown.md` (ar-21 `91e7b250`, ar-22 `a588a242`, ar-23 `3a54ea91`, AC9 → closed, fix `:382-385` "awaiting owner decisions D1–D3"); `documents/solution/assignment-request-implementation-details.md` §3 round log. Do not chase `src/Config/…` (empty dir, nit-10).

**Row 7 — directory fail-open (P1-1b).**
- `src/Assignments/SchoolCollab.Assignments.Api/Program.cs:109,116`: `.ConfigurePrimaryHttpMessageHandler(() => new HttpClientHandler { AllowAutoRedirect = false })` on both named clients (a real-auth challenge must surface as 401/302-failure, never be followed to a 200 login page; `IsSuccessStatusCode` is already strict-2xx). APIs never legitimately redirect, so no hop regresses.
- Test: scripted-`HttpMessageHandler` unit test (repo rule — never Moq) on `TeacherDirectoryHttpClient.ExistsAsync` with a scripted **302**, **404**, and **200** — asserting `false`/`false`/`true` (regression coverage of the strict-2xx path; **this test alone cannot discriminate `AllowAutoRedirect=false`** — a scripted handler never follows redirects — P1-6). The **discriminating proof is the source-scan assertion in row 3's guard** (check 4: `ConfigurePrimaryHttpMessageHandler` + `AllowAutoRedirect = false` on both `Assignments.Api/Program.cs` clients); AC5 and revert probe (c) reference THAT assertion.

### Resolved decisions

| Decision | Resolution |
|---|---|
| **R3 — foreign-tenant outcome** | Typed `TeacherTenantMismatchException` (Core/Domain/Exceptions) mapped at every affected route to `Results.Problem(ex.Message, statusCode: 403)` — consistent with the sibling `UnauthorizedAccessException → 403` handler (`AssignmentRoutes.cs:800-802`) and the five `MissingTeacherPrincipalException → 403` catches. 403, not 409 (409 is a state conflict, wrong semantics), no shape change. |
| **R4 — review-queue route param** | The route resolves the teacher id principal-first (`currentUser.TeacherId ?? (realAuth ? throw : routeParam)`) and passes the resolved id into the unchanged `GetSubmissionsForReview(Guid)` record — no contract change, no consumer impact (verified: no `src` consumer calls review-queue). |
| **Row 2 mechanism** | `AuthenticationStateProvider` in the component (circuit principal → `teacher_id` claim); `Guid.Empty` fallback when no claim (dev posture unchanged); all four sites. |
| **Row 7 design** | `AllowAutoRedirect = false` on both API-side named clients + the existing strict-2xx check; scripted 302/404/200 = strict-2xx regression coverage; **discriminating proof = the guard's source-scan assertion** (P1-6). |
| **Handler home / registration** | `Core/Auth/BearerForwardingDelegatingHandler.cs` + `TryAddTransient` in `AddAuthAndTenancy`; attachments **bounded to the API-to-API hops** (P1-7); worker carve-out; Blazor-side clients = named residual (no circuit-safe token source in this round). |

### Final ACs

1. **AC1** — claim-wins exists on **all four** handlers + the queue route resolves principal-first; foreign-tenant claim → `TeacherTenantMismatchException`; queue route → **403 never 500**; TestAuth wire-field honoured. Tests: `SubmissionReviewIdentityTests.cs` (5 discriminating cases) + the row-4 harness's queue 403 case.
2. **AC2** — the shared handler exists in Core/Auth, is attached to the **API-to-API hop clients** (guard-pinned; per P1-7 the interactive Blazor clients are a **named residual** — cookie-session token is the only source there), no-op in dev bypass (unit test with a **stubbed HttpContext**: flag=true → header absent; flag=false + current-request bearer → header forwarded — attachment effectiveness is pinned by the guard, not end-to-end), **worker carve-out pinned by the guard**; the students/settings groups — **including `/teachers`** — opt into Bearer **per group** (guard-pinned + exercised by AC3).
3. **AC3** — flag=false **401 with no token** passes for the assignments host (RealAuthRouteTests with `AddAuthAndTenancy` + in-memory flag=false config) and one representative route per students/settings host (Integration ApiFactory with the **env-var flag override**).
4. **AC4** — dev `staff_number` seeded (`DEV-0001`); the seeder architecture test asserts it.
5. **AC5** — directory fail-open closed: scripted 302 → `ExistsAsync == false` (strict-2xx regression); the `AllowAutoRedirect=false` wiring is **guard-pinned by the source-scan assertion (P1-6)**.
6. **AC6** — build 0 errors; **whole matrix ≥ 2,391, 0 failures**; ArchitectureTests ≥ 29/0 + the new guard; Core 92/0; no contract-shape change, no migration, CPM untouched, no new secrets, flags centralized; both registry files + configuration.md updated.

### Expected files (freeze/diff-review baseline)

| File | Row |
|---|---|
| `src/Assignments/SchoolCollab.Assignments.Core/Domain/Exceptions/TeacherTenantMismatchException.cs` **(new)** | 1 |
| `src/Assignments/SchoolCollab.Assignments.Core/CQRS/Assignments/Commands/ReviewSubmission/ReviewSubmissionCommandHandler.cs` | 1 |
| `…/Commands/ReviewAssignmentCommand/ReviewAssignmentCommandHandler.cs` | 1 |
| `…/Commands/ApproveAssignmentCommand/ApproveAssignmentCommandHandler.cs` | 1 |
| `…/Commands/OverrideStudentSubmissionAttempts/OverrideStudentSubmissionAttemptsCommandHandler.cs` | 1 |
| `src/Assignments/SchoolCollab.Assignments.Api/Endpoints/AssignmentRoutes.cs` | 1 |
| `src/Assignments/SchoolCollab.Assignments.Application/Components/Pages/Assignments/Detail.razor` | 2 |
| `src/SchoolCollab.Core/Auth/BearerForwardingDelegatingHandler.cs` **(new)** | 3 |
| `src/SchoolCollab.Core/Auth/AuthTenancyExtensions.cs` | 3 |
| `src/Assignments/SchoolCollab.Assignments.Api/Program.cs` | 3, 7 |
| `src/Settings/SchoolCollab.Settings.Application/ModuleServices.cs` | 3 |
| `src/Students/SchoolCollab.Students.Api/Program.cs` | 3 |
| `src/Students/SchoolCollab.Students.Api/StudentEndpoints.cs` | 3 |
| the 8 Settings.Api `*Endpoints.cs` files + `ConfigFlagRoutes.cs` + `ConfigResolveRoutes.cs` (**10 — as built**; no `SettingsEndpointAuth.cs` helper). `Settings.Application/ModuleServices.cs` deliberately **untouched** (P1-7 residual) | 3 |
| `tests/SchoolCollab.Assignments.Tests.Unit/SubmissionEngineTests.cs` (3 ctor sites `:242/:257/:299` — P1-4) | 1 |
| `src/SchoolCollab.MigrationService/Seeding/DevIdentitySeeder.cs` | 5 |
| `tests/SchoolCollab.Assignments.Tests.Unit/SubmissionReviewIdentityTests.cs` **(new)**; `AssignmentDetailBunitTests.cs` (extend) | 1, 2 |
| `tests/SchoolCollab.Assignments.Api.Tests.Unit/RealAuthRouteTests.cs` **(new)** | 4 |
| `tests/SchoolCollab.Students.Tests.Integration/*` + `tests/SchoolCollab.Settings.Tests.Integration/*` (one 401 test each) | 4 |
| `tests/SchoolCollab.ArchitectureTests.Unit/BearerForwardingWiringArchitectureTests.cs` **(new)**; `DevIdentitySeederStaffNumberArchitectureTests.cs` **(new)** | 3, 5 |
| scripted-handler test for `TeacherDirectoryHttpClient` — **landed at `tests/SchoolCollab.Assignments.Api.Tests.Unit/TeacherDirectoryHttpClientTests.cs`** | 7 |
| `documents/configuration.md`; `documents/solution/assignment-request-go-forward-breakdown.md`; `documents/solution/assignment-request-implementation-details.md` | 6 |

### Execution order

1. Rows 1 → 3 → 7 → 5 (code), then rows 2 (UI) and the tests, then row 4's harness tests, then row 6 (docs) **last**.
2. `dotnet build SchoolCollab.sln` (plain — must be 0 errors) **after the code rows**, then the full unit matrix per repo discipline (one grep-filtered read per suite).
3. **Bounded runtime check BEFORE the patch freeze (P2-4 decision)**: with the AppHost working on this machine (runbook + hidden-window launch recipe), flip `FeatureFlags__FEATURE__DisableOIDCAuth=false`, mint a `dev-teacher` ROPC token (configuration.md §4 recipe), and record verbatim: `GET /assignments` **200** with the token (acceptance on the already-opted assignments group) and **401** without; plus one representative students-api/settings-api protected route **200 with token / 401 without** (proves the new receiving-side opt-in end-to-end). ~5 min, decomposed per the ar-21 precedent; if the AppHost cannot start, record that as a residual instead of improvising. **STATUS (pass 3): DEFERRED-RESIDUAL — Docker Desktop's container runtime entered Resource Saver mode ("containers may not run"); no containers → no DB/Keycloak → the check could not run. Verbatim evidence in the worker report. Re-run when the runtime is healthy (first try waking it: `docker start ar10repro` — Resource Saver auto-resumes when work arrives); if the engine is genuinely down, that is an owner action.
4. Revert probes (each proven discriminating, then restored byte-identical): (a) row 1 — revert one cross-check → AC1 test fails; (b) row 3 — remove the `Program.cs:109` attachment → guard fails; remove the `/teachers` opt-in → guard fails; (c) row 7 — remove `AllowAutoRedirect=false` → **the guard's source-scan assertion fails** (P1-6; the scripted-302 test alone cannot discriminate); (d) row 5 — restore the `NULL` → seeder test fails.
5. **End-to-end honesty statement (P2-4)**: the runtime check above proves bearer *acceptance* on the opted-in groups; bearer *forwarding* effectiveness is pinned by the guard + the stubbed-HttpContext unit test only. Say both, exactly, in the worker report.
6. Worker constraints (binding, verbatim intent): **30-minute cap with a cut-line** — at the cap, write the report with done/remaining and stop; **mandatory report** to `documents/rounds/.ar-24-worker-report.md` (files, per-suite counts, pinned anchors, deviations, residuals); **NEVER edit the round doc**; no commit/push/`gh`; plain `dotnet build SchoolCollab.sln`; exact per-suite `dotnet test tests/<Name>` commands judged by exit code + the `total:/failed:` line, never truncated logs; never `find /`, never obj/bin/`~/.nuget`; ENOSPC → stop and report; kill stray `dotnet` processes before builds (PowerShell `Name`-based filter, never matching its own shell).

### Gates

No contract-shape change (records/messages untouched) · no migration · CPM untouched · no new secrets · flags stay on the canonical key · TestAuth/dev corpus must stay green (R5) · whole matrix ≥ 2,391 / 0 failures · UI diff is behaviour-only (no visual change ⇒ no UI tester, per the ar-21 deterministic trigger).

## Plan review (run `e806815d`, 2026-09-21 — REWORK 7 P1 + 5 P2 + 3 nits — ALL LANDED)

> Full verbatim block preserved in the run's retention dir (`…\async-subagent-runs\e806815d-9cdf-483d-a3ce-e46baaa4927c`). **Landing map:** P1-1 → Named-clients anchor rewritten with reviewer-verified sites (Families registers via the `AddCrossModuleHttpClient` helper, not `AddHttpClient`; Admin registers none) + row 3 attachments bounded + expected-files fixed; P1-2 → `/teachers` added to row 3/AC2 and the guard made per-group (5 blocks + 8 files); P1-3 → cross-check source pinned per handler (`submission.TenantId` for Override — it never loads an assignment); P1-4 → row 1 states the arity change, `SubmissionEngineTests.cs` (3 ctor sites) added to expected files, "do not alter existing test tenants" noted; P1-5 → row 4 harness rewritten (`AddAuthAndTenancy` + in-memory flag=false config; env-var override for the two ApiFactories that hard-force `true`); P1-6 → row 7's discriminating proof moved to the guard's source-scan assertion (a scripted handler never follows redirects, so the scripted test cannot discriminate); P1-7 → row 3(a)/AC2 **bounded to the API-to-API hops** (the handler reads `IHttpContextAccessor`, which is null in interactive circuits, and the browser carries the cookie, not a bearer) — Blazor-side clients recorded as a named residual; P2-1/2/5 → anchors corrected (`:11-14/:36-39/:48-51/:55-58/:65-68`; the "settings→students" label was wrong; OR-1 deleted); P2-3 → foreign-tenant test parameterized over all four handlers; P2-4 → **one bounded runtime check added before the freeze** (flag=false + ROPC token → 200-with/401-without on the opted-in groups) + an end-to-end honesty statement; nit-1/2/3 → landed inline (`:801-803`; dev-UI review path stays broken by design; seeder NULL = column 9). Reviewer-verified: the orchestrator's three corrections were **confirmed** (routes path, four `Guid.Empty` sites, `ReviewSubmissionCommandHandler` has no claim-wins). After landing: **ready to dispatch.**

## Worker passes (4 — cap-split per the row-1b precedent)

- **Pass 1 (cap hit):** rows 1, 2, 5, 7 + row-3 Assignments/Students side; build 0 errors; 663/0 + 92/0.
- **Pass 2:** Settings opt-in + **guard written & revert-proven** (ArchitectureTests 34/0); Assignments 664/0.
- **Pass 3:** AC1 `SubmissionReviewIdentityTests` (foreign-tenant ×4) + row-7 scripted test + seeder test + the 2 extra Settings sites (parent decision); 4 suites green.
- **Pass 4:** AC2 handler unit test + row-6 docs (configuration.md + both registry files) + **full matrix ≈2,412/0**. Report: `.ar-24-worker-report.md` (238 lines, 4 passes).
- **Environment residual:** Docker Desktop **Resource Saver** mode blocked the pre-freeze runtime check and row 4 — recorded verbatim, not improvised.
- **Pass 5 (final, focused):** row-4 route tests on the container-free TestServer harness found by the diff reviewer; Assignments.Api.Tests.Unit **87/0** (83+4).

## Parent verification (independent — Tier 3 leans has no separate acceptance agent for this)

The parent re-ran the affected suites itself after landing the diff-review findings and pass 5: `dotnet build SchoolCollab.sln` **0 errors**; ArchitectureTests **34/0**; Assignments.Tests.Unit **672/0**; Assignments.Api.Tests.Unit **87/0**; Core **92/0**; worker-reported whole matrix ≈**2,416/0**. **Revert probe (b) re-run by the parent** after the guard became count-based: removing ONE of the two `Assignments.Api` `AddHttpMessageHandler<BearerForwardingDelegatingHandler>()` attachments → `AssignedAttachments_Present_WorkerAbsent` **failed** (1/34); file restored **byte-identical** (`cmp`) → **34/0**.

## Diff review (run `3531f117`, 2026-09-21 — **ACCEPT, 0 P1**; 4 P2 + 6 nits — ALL LANDED by the parent)

> Full verbatim block in the run's retention dir (`…\3531f117-f6d8-44ba-ac6e-f26a4ee5c814`). **Landing map:** P2-1 → guard attachment checks made **count-based** (2/3/0 — a single removed attachment now fails the guard, fixing revert probe (b)); P2-2 → breakdown `:383-385` SCOPED/awaiting-D1–D3 clause replaced with the merged state and `:341`'s AC9 residual marked **CLOSED by ar-22**; P2-3 → `RejectAssignment` (claim-wins, no cross-check — the fifth write path) recorded as a **named residual** in `configuration.md` + this doc (defense-in-depth only; EF global tenant filter makes it non-exploitable); P2-4 → AC3 re-scoped to the **container-free TestServer harness** the reviewer found (`SignOffRoutesTests.cs:54-83` — "Program is never started"), pass 5 dispatched; nit-5 → exception message names `assignment.Id, "assignment"`; nit-6 → seeder test asserts the ordered fragment `'Dev Teacher', NULL, 'DEV-0001', NULL`; nit-7 → implementation-details branch clause completed; nit-8 → `ConfigEndpoints.cs` comment cookie→bearer-gated; nit-9 → `Students.Worker/Program.cs:53` added to the named residuals; nit-10 → expected-files bookkeeping (this edit) + revert-probe honesty: probes (a)/(d) not run — discrimination by construction — and probe (b) **re-run after the count fix**.

## Acceptance

| AC | Verdict |
|---|---|
| AC1 claim-wins + cross-check | **PASS** — all four handlers + queue route principal-first; 5 discriminating test methods; no existing test tenant altered |
| AC2 bearer forwarding + receiving opt-in | **PASS** — handler unit-tested (no-copy/copy/no-throw), guard count-based + revert-proven, 15 opt-in sites (5 students + 10 settings) |
| AC3 flag=false route tests | **PASS** — `RealAuthRouteTests.cs` on the container-free TestServer harness; **4 × 401-no-token** (`/assignments`, ward list, review-queue, approve). **Discriminating**: under real-auth a missing Bearer opt-in would challenge via the `DefaultScheme = Cookie` (302 redirect), so asserting **401** tests the opt-in itself, not merely "some auth exists". Students/settings 401 = declared residual (no cheap container-free harness in those 1-test projects); their opt-in stays guard-pinned by the 10-site guard. |
| AC4 dev `staff_number` | **PASS** — seeded `DEV-0001` + ordered-fragment architecture test |
| AC5 directory fail-open | **PASS** — `AllowAutoRedirect=false` guard-pinned + scripted 302/404/200 |
| AC6 hygiene | **PASS** — build 0 errors; matrix ≈2,412/0; no contract/migration/CPM/secret change; both registry files + configuration.md updated |
| Runtime check (bearer acceptance) | **RESIDUAL — Docker Resource Saver** (environment) |

**Round verdict: CLOSED — diff review ACCEPT 0 P1; all findings landed; AC1–AC6 covered (1,2,4,5,7 code+test, 3 via the container-free harness).** Two declared residuals are **environment-gated, not diff-gated**: (a) the students/settings 401 route evidence and (b) the pre-freeze runtime check (flag=false + ROPC token → 200-with/401-without) — both need Docker Desktop out of **Resource Saver** mode. Named design residuals: the Blazor-side bearer gap (no circuit-safe token source), worker→settings/students 401s post-flip, and `RejectAssignment`'s missing tenant cross-check (defense-in-depth). Frozen patch: `diffs-ar-24-identity-hardening.patch` (34 files, +996/−36).
