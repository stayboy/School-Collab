Provider: pi/clinepass — round began on the legacy `ollama` ladder, then switched by owner directive mid-round (2026-09-16): orchestrator glm-5.3-flash, worker deepseek-v4-flash-0731, reviewer deepseek-v4.1-flash [owner override], ui-tester minimax-m3, higher-model re-verify glm-5.3 — Tier 3 FULL FOUR-AGENT, Phase 3 slice WS-F3 (guardian sign-page relocation behind real auth + E1 deep links)

# Round ar-15-signoff-relocation (WS-F3)

**Round base:** cut `stack/15-ar-15-signoff-relocation` from the stack/14 tip `feac20a4` (the unmerged-tip precedent — train is main ← stack/13 (#236) ← stack/14 (#237); F3 touches Families + Assignments API files from ar-13/ar-14, so main is not an option until the train merges; **retarget at PR time**).

**Sources read & verified at plan time:** spec §3.2 (signature/completion chain, audit), §2 (Guardian persona), §6 NFR security; breakdown Phase 3 WS-F3 row + §2 (WS-F, D-4, D-6); impl-details §2 WS-C + WS-F; `rounds/round-ar-9-signoff.md` (v1 posture + self-select decisions); `rounds/round-ar-14-deep-links.md` (E1 decisions (a)–(i) + deployment-smoke residuals); tree verification of `SignOff.razor`, `AssignmentRoutes.cs`, `SignOffSubmissionCommand`, the `Core/DeepLinks` trio, `DeepLinkLandingService`, `DeepLinkEndpoints`, `FamiliesApiClient`, `IStudentDirectory`, `ISubmissionRepository`, `CertificateDownloadService`, Families `Program.cs`/pages/wwwroot; `.github/copilot/rules/dotnet-best-practices.md` + `blazor-components.md`; skills **bounded-context**, **dialog-ui** (both exist).

## Plan

### Goal

Relocate the guardian e-sign surface (WS-C2's v1 page, currently the Admin-hosted `SignOff.razor` inside the Assignments Application RCL with the acting-guardian **self-select** dev posture) behind real auth: a **token-validated guardian route group at the Assignments API** extending the ar-14 E1 protector, and a **guardian-facing sign page in the Families host** consuming the sign-off context + sign command + certificate download. The teacher surface (SignOffSection card, reassign, finalize) stays in Admin untouched. Route/command shapes for the existing teacher/admin group unchanged.

### Decisions (binding — owner-adopted)

- **(a) Guardian API auth = a token-validated guardian route group at the API**, extending the E1 protector: the Families host forwards a **re-minted `ar-deeplink` token** on the guardian call family via a request header (`x-deeplink-token`); the API validates it with the **shared keyring + the existing `DeepLinkConstants.Purpose`** (`"ar-deeplink"`), checks the token's (assignmentId, wardStudentId?) scope against the route, resolves the acting guardian **server-side** from the token's ContactId, and authorizes. Commands unchanged. **NO new keyring, NO new purpose string, NO OIDC dependency.**
- **(b) The acting guardian binds to the token** — no self-select on the deep-link path. The Families page uses the token-resolved guardian; the request body on the guardian route carries **NO GuardianId**. The self-select UI survives ONLY as the dev/TestAuth fallback on the existing Admin-hosted page (`SignOff.razor`, which remains the dev posture surface, untouched).
- **(c) Families hosts the relocated page**: a guardian sign page in the Families host (D-4 guardian-facing surface) consuming the sign-off context + sign command + certificate download. Teacher surfaces (SignOffSection in Detail.razor's Guardian Sign-off tab, reassign, finalize) stay in Admin **untouched**.
- **(d) Route/command shapes unchanged; NEW guardian-scoped API routes** as a **parallel public route group reusing the SAME command/query handlers** (dual-auth on the existing teacher routes rejected — it churns the teacher surface's auth matrix for no gain). The existing group (`AssignmentRoutes.cs`) is untouched.
- **(e) Redirect rules extended as needed** (recorded in step 6): guardian tokens (`OwnerType == Guardian`) with `WardStudentId` set land **directly on the sign page** for that (assignment, ward); multi-ward guardians land on `/ward` and reach sign-off per ward via the ward surface; **student-owned token behavior unchanged** (`WardStudentId set → /ward/{sid}`).
- **(f) Out of scope (record, do not touch):** E2/E3 (email delivery — decisions recorded separately in the breakdown), D-6 ward/guardian OIDC identities, the **systemic** service-to-service credential fix (recorded residual — this round fixes only the sign-route family), teacher UI, rubrics/comments, drawn signature (Phase 5).

### To-verify resolutions (all resolved at plan time — the worker starts from these)

1. **Token forwarding/re-mint mechanism (header seam, mirroring `x-tenant-id`):** the ar-14 landing consumed the raw token into a cookie principal carrying `contact_id` + `tenant_id` claims (verified: `DeepLinkEndpoints.SignInAsync`) — the payload is NOT in the cookie. Therefore: **the Families host re-protects a `DeepLinkTokenPayload` per guardian call** via the registered `DeepLinkProtector` (already in Families DI from ar-14) and `FamiliesApiClient` attaches it as the `x-deeplink-token` header — the exact seam shape of the existing `x-tenant-id` seam (`MarkRecipientOpenedAsync`'s explicit header honored by `TenantPropagationDelegatingHandler`). Payload fields: `TenantId`/`ContactId` from the cookie principal (bridged by `TenantClaimsTransformation` → `ITenantProvider`/claims), `AssignmentId`/`WardStudentId` from the page parameters, `OwnerType` = guardian, `ExpiresAt` = now + a short re-mint TTL (suggest 15 min, constant next to the client). **The re-mint cannot extend validity: the API middleware cross-checks the authoritative `AssignmentRecipient.DeepLinkExpiresAt` from the store (res. 3).** No new keyring, no new purpose — `Protect` uses the same `DeepLinkConstants.Purpose` wrapper.
2. **Guardian route-group shape at the API:** new `src/Assignments/SchoolCollab.Assignments.Api/Endpoints/GuardianSignOffRoutes.cs` — `MapGuardianSignOffRoutes(this WebApplication app, ...)` extension (endpoint-grouping rule; no inline routes in `Program.cs`), mapped **outside the teacher/admin-gated group with NO `RequireAuthorization`** (the token header is the credential — the ar-14 `/deeplink` public-group precedent). Routes (parallel, reusing the SAME handlers):
   - `GET /guardian/assignments/{id:guid}/students/{studentId:guid}/sign-off` → `GetSignOffContextQuery` (reuse; same 404 mapping).
   - `POST /guardian/assignments/{id:guid}/students/{studentId:guid}/sign-off` → body = **NEW additive contract record `GuardianSignOffSubmissionRequest(SignatureTypeDto SignatureType, string? TypedSignature = null)`** (NO GuardianId — decision (b)); the endpoint builds `SignOffSubmissionCommand(id, studentId, **token-resolved GuardianId**, type, typed, ip, userAgent)` — the handler's existing fail-CLOSED `IsGuardianOfAsync` link validation stays as defense-in-depth. Existing `SignOffSubmissionRequest` and the teacher route untouched (the teacher route keeps taking `GuardianId` from the body — that is the dev/TestAuth posture).
   - `GET /guardian/assignments/{id:guid}/students/{studentId:guid}/certificate` → same logic as the existing certificate route (extract a small shared private helper or duplicate the ~25-line block — worker's call, reported as a deviation either way).
   - **Reassign/finalize are NOT exposed on the guardian group** (teacher-only, decision (c)).
   - Error-mapping parity: 401 (missing/invalid/expired/wrong-purpose header token), 403 (scope mismatch: wrong assignment / wrong ward / tenant mismatch / recipient row not guardian-owned), then the existing sign route's 404/409/400 mapping verbatim (assignment/submission 404; `SubmissionAlreadySigned`/`SignOffState`/`Locked`/`GuardianNotAuthorized` 409; `ArgumentException` 400). Naming: the `guardian/` prefix keeps the public namespace disjoint from the gated `assignments` group.
3. **Contact→guardian resolution in the API middleware — via the recipient row, in-process (plan correction to drift fact 6):** `IStudentDirectory` has **NO ContactId→GuardianId lookup** (verified: `GetGuardiansAsync` returns `WardGuardianInfo(GuardianId, DisplayName, IsPrimary)` — no contact). The resolution path is **`ISubmissionRepository.GetRecipientAsync(assignmentId, contactId)`** (verified existing, used by the publish handler) — the `AssignmentRecipient` row keyed (assignment, contact) carries `OwnerType`/`OwnerId`/`WardStudentId`/`DeepLinkExpiresAt`. The middleware/filter: unprotect → expiry → tenant (`TenantId` from the token; establish explicit tenant context before the repo call) → `GetRecipientAsync` → **row must exist, `OwnerType == ContactOwnerType.Guardian`, `OwnerId` = acting GuardianId, `WardStudentId` null-or-matching the route studentId, `DeepLinkExpiresAt > now`** (the authoritative server-side expiry — a re-minted header token cannot outlive the stored link). 401 for token failure, 403 for scope/row mismatch. *Worker verify at impl time:* `OwnerId` for guardian-owned rows is the Guardian entity id (the publish handler sets it from `SubscriberInfo(ContactOwnerType, OwnerId, …)`); the handler's `IsGuardianOfAsync` re-validates the link regardless, so a wrong mapping fails closed as 409 — but the filter test must assert the correct id is threaded.
4. **Feature flag: NO new flag.** The guardian route group and the Families sign page ship behind the **existing tenant runtime flag `FEATURE:EnableDeepLinks`** (same dark-launch posture as the landing: flag off → 401 on the guardian group, dark on the Families side). No config-documentation §2 flag row is needed; add a one-line configuration.md note that the WS-F3 guardian routes are gated by the same flag (config-documentation rule satisfied by documentation-of-reuse).
5. **Certificate download path:** the Admin path (`AssignmentsApiClient.GetCertificateAsync → byte[]` + `wwwroot/js/fileDownload.js` interop) is Admin-side only — Families `wwwroot/js` has just `wardPlayer.js` (verified). The Families guardian page gets its **own** download path: `FamiliesApiClient.GetGuardianCertificateAsync(assignmentId, studentId, …) → Task<byte[]>` (against the guardian certificate route — a plain `<a href>` cannot attach the `x-deeplink-token` header) + a Families-side `GuardianCertificateDownloadService` mirroring `CertificateDownloadService` line-for-line (lazy dynamic `import()`, module ref owned by the consumer, `IAsyncDisposable`, `JSDisconnectedException`-safe) + **Families' own `wwwroot/js/fileDownload.js`** (copy of the Admin module — same JS-interop pattern).
6. **`SignOffContextDto` sufficiency: VERIFIED — no contract change needed.** The DTO carries everything the RCL page consumes (title, student name, state, signedAt, finalizedAt, version, score, passed, consent text, Guardians). `WardGuardianDto(GuardianId, DisplayName, IsPrimary)` has no ContactId — irrelevant here: the Families page never picks a guardian (the API resolves it from the token). The **only** contracts change is the additive `GuardianSignOffSubmissionRequest` record (new record; no existing record gains or loses members).
7. **Test placement:** token-filter + guardian-route tests → `tests/SchoolCollab.Assignments.Api.Tests.Unit/`; Families page bUnit + client re-mint/header tests → `tests/SchoolCollab.Families.Tests.Unit`; purpose/app-name parity → the existing `DeepLinkProtectorTests` (Core) + the `DotNetBestPracticesArchitectureTests` keyring source assertion (already asserts both hosts' registration — extend only if the assertion needs the new files, else untouched).

### Expected files (~18 ±4)

**Created:**
1. `src/Assignments/SchoolCollab.Assignments.Api/Endpoints/GuardianSignOffRoutes.cs` — the public guardian route group (extension method).
2. `src/Assignments/SchoolCollab.Assignments.Api/Auth/GuardianTokenEndpointFilter.cs` (name/folder at the worker's discretion — filter or middleware) — header token validation + scope checks + recipient-row cross-check.
3. `src/SchoolCollab.Families/Components/Pages/Ward/SignOff.razor` — the guardian sign page (`@page "/ward/{StudentId:guid}/assignments/{AssignmentId:guid}/sign-off"`).
4. `src/SchoolCollab.Families/Services/GuardianCertificateDownloadService.cs` — the JS-interop download orchestration.
5. `src/SchoolCollab.Families/wwwroot/js/fileDownload.js` — copy of the Admin interop module.
6. `tests/SchoolCollab.Assignments.Api.Tests.Unit/GuardianTokenEndpointFilterTests.cs` — valid/expired/tampered/wrong-purpose/wrong-assignment/wrong-ward/tenant-mismatch/not-guardian-recipient.
7. `tests/SchoolCollab.Assignments.Api.Tests.Unit/GuardianSignOffRoutesTests.cs` — happy path via the SAME handlers; body carries NO GuardianId; error-map parity.
8. `tests/SchoolCollab.Families.Tests.Unit/GuardianSignOffPageTests.cs` — bUnit.
9. `tests/SchoolCollab.Families.Tests.Unit/GuardianTokenHeaderTests.cs` — re-protect/header attach.

**Modified:**
10. `src/Assignments/SchoolCollab.Assignments.Contracts/ContractTypes.cs` — additive `GuardianSignOffSubmissionRequest` only.
11. `src/Assignments/SchoolCollab.Assignments.Api/Program.cs` — `app.MapGuardianSignOffRoutes()` + filter DI.
12. `src/SchoolCollab.Families/Services/FamiliesApiClient.cs` — three guardian methods + `x-deeplink-token` attach (constructor gains `DeepLinkProtector`).
13. `src/SchoolCollab.Families/Program.cs` — DI for the download service (+ verify `DeepLinkProtector` registration is reusable for outgoing calls).
14. `src/SchoolCollab.Families/Components/Pages/Ward/Assignment.razor` (or `Index.razor`) — the "Sign off" affordance when the ward's submission awaits signature (worker's choice of surface; both acceptable, report).
15. `src/SchoolCollab.Families/DeepLinks/DeepLinkLandingService.cs` — decision (e) redirect extension (guardian + `WardStudentId` → sign page; multi-ward → `/ward`; student behavior unchanged).
16. `tests/SchoolCollab.Families.Tests.Unit/DeepLinkLandingServiceTests.cs` — new redirect-rule cases.
17. `documents/configuration.md` — one-line WS-F3 flag-reuse note (§2 area).
18. `documents/solution/assignment-request-go-forward-breakdown.md` — WS-F3 row → landed (post-acceptance, orchestrator; not part of the worker diff).

No `Directory.Packages.props` change (zero new packages — everything needed shipped in ar-14). No migration. No AppHost change. No new flag/`FeatureFlagKeys` constant.

### Implementation steps (ordered; `dotnet build SchoolCollab.sln` after EVERY step; multi-pass expected — cut-line protocol per pass)

1. **Contracts (decision (d)/(b)).** Add `GuardianSignOffSubmissionRequest(SignatureTypeDto SignatureType, string? TypedSignature = null)` to `ContractTypes.cs` — additive only, existing records untouched. Build.
2. **API token filter (to-verify 2/3).** `GuardianTokenEndpointFilter`: read `x-deeplink-token` → `DeepLinkProtector.TryUnprotect` (shared purpose) → `ExpiresAt` check → explicit tenant context from `payload.TenantId` → `GetRecipientAsync(payload.AssignmentId, payload.ContactId)` → guard `OwnerType == Guardian`, `WardStudentId` scope, `DeepLinkExpiresAt > now` → stash the resolved `GuardianId` (from `OwnerId`) in `HttpContext.Items` for the endpoint. Unit tests (all nine cases). Build + `dotnet test tests/SchoolCollab.Assignments.Api.Tests.Unit`.
3. **API guardian route group (decision (a)/(d)).** `GuardianSignOffRoutes.cs` (extension method, public, no `RequireAuthorization`): GET context / POST sign (thread the `Items`-resolved GuardianId, **never** the body) / GET certificate, with the parity error mapping. Wire `MapGuardianSignOffRoutes` into `Program.cs`. Route tests incl. the "body must not carry GuardianId" assertion. Build + both Assignments test projects.
4. **Families client seam (to-verify 1).** `FamiliesApiClient`: `GetGuardianSignOffContextAsync`, `SubmitGuardianSignOffAsync` (body = the NEW record), `GetGuardianCertificateAsync`; each attaches `x-deeplink-token` = `DeepLinkProtector.Protect` of a short-TTL payload built from the cookie principal (`contact_id`, `tenant_id`) + call args. Constructor gains `DeepLinkProtector`. Header tests. Build + Families tests.
5. **Families guardian page (decisions (b)/(c)).** `Ward/SignOff.razor`: loads the context (ONE call), review summary, consent card gating the Sign button (ar-9 UX, **minus the self-select** — signing identity is implicit; optionally a read-only "Signing as <link>" line when resolvable), typed/click signature per the ar-9 pattern, success/error bars, the already-signed read-only re-entry posture, `GuardianCertificateDownloadService` + `fileDownload.js` for the finalized-certificate affordance. Styling: existing utility classes + FluentUI only — no inline style, no new `.razor.css` (the ar-9 round decision (h) posture). bUnit tests. Build + Families tests.
6. **Landing redirect extension (decision (e)).** `DeepLinkLandingService`: `OwnerType == Guardian` + `WardStudentId` set → `/ward/{wardStudentId}/assignments/{assignmentId}/sign-off`; guardian without `WardStudentId` → `/ward` (unchanged); student tokens unchanged. Update landing tests. Build + Families tests.
7. **Surface affordance + docs.** The ward-assignment/ward-list "Sign off" affordance (step 5's item 14); `documents/configuration.md` one-line flag-reuse note. Build; run the FULL unit matrix (`dotnet test` on Core, Assignments, Assignments.Api, Families, Students, Settings, Admin, ArchitectureTests).

### Binding test coverage list (MSTest + FluentAssertions; bUnit for components)

- **Token filter** (`GuardianTokenEndpointFilterTests`): valid token passes and yields the recipient-resolved GuardianId; expired (`ExpiresAt` past) → 401; tampered/malformed → 401; wrong-purpose token → 401; wrong assignment scope → 403; wrong ward (`WardStudentId` mismatch) → 403; tenant mismatch → 403; recipient row absent → 403; row `OwnerType != Guardian` → 403; stored `DeepLinkExpiresAt` past (even with a fresh re-minted header token) → 403 (the authoritative-expiry cross-check).
- **Guardian route happy path** (`GuardianSignOffRoutesTests`): reuses the SAME `GetSignOffContextQuery`/`SignOffSubmissionCommand` handlers (assert the handler receives the **token-resolved** GuardianId from `HttpContext.Items`, NOT a body-supplied one — the request body must NOT carry GuardianId on the guardian route); error-map parity (404/409/400 identical to the existing sign route); reassign/finalize absent from the group; certificate route returns the PDF stream / 404 parity.
- **Families page bUnit** (`GuardianSignOffPageTests`): context render; sign flow (typed + click); certificate affordance (post-finalize); error states (404, 409 already-signed → read-only re-entry); **NO self-select element anywhere in the Families page**.
- **Wiring**: keyring reuse asserted via the existing `DotNetBestPracticesArchitectureTests` deeplink source assertion (both hosts) — extend only if needed; a purpose/app-name parity test (Core `DeepLinkConstants` used by both the mint path and the new filter + client header); `DeepLinkLandingServiceTests` new redirect-rule cases; contract round-trip (`GuardianSignOffSubmissionRequest` string-enum serialization parity with `FamiliesJson.Options`).
- **Existing suites untouched-green** (plan-time counts): Core 84, Assignments 597, Assignments.Api 58, Families 30, Students 422+1, Settings 519+1, Admin 546, ArchitectureTests 21.

### Constraints (repo AGENTS.md — restated for the worker)

- `dotnet build SchoolCollab.sln` after **every** step; fix compiler errors before continuing; MSB3021/3027 locks → stop the offending process, surface, don't loop.
- CPM: NO new packages this round; any `PackageReference` must omit `Version` (CPM-managed).
- Target net10.0; no downgrades.
- No direct project references between bounded contexts — Families uses `Assignments.Contracts` + `SchoolCollab.Core` only (the ar-13 Option B boundary); no MediatR — `ICommandHandler`/`IQueryHandler` via the existing handlers.
- API endpoints grouped via extension methods, not inline in `Program.cs`; conditional auth per `IFeatureFlagService` where applicable (here: the public group's auth is the token itself, recorded).
- Blazor: FluentUI components + existing utility classes only; no `<style>` blocks, no inline `style=`, no new `.razor.css` (the ar-9 page posture); dropdown/select work per the dropdown-ui skill; dotnet-best-practices rule file read before any `.cs`/`.razor` edit (CI-enforced via `SchoolCollab.ArchitectureTests.Unit`).
- Tenancy: operational-data pattern; tenant context established explicitly from the token payload (the ar-14 `RunWithExplicitTenantAsync` precedent); tenant-isolated cache keys unchanged.
- Domain entities: PostgreSQL `xmin` row version untouched (no new entities this round — **no migration**).
- Existing migrations, teacher/admin surfaces, contracts records, the ar-14 landing cookie/sign-in shape: untouched.
- Boundaries: repo-scoped searches only; a check resisting >3 minutes → mark "unverifiable statically — flagged" and move on.

### Acceptance criteria

- **Worker-facing:** build 0 errors; full unit matrix 0 failures; changed files = expected list ±4; every deviation reported (not silent); the contracts diff is exactly the ONE additive record; the existing teacher/admin sign routes and `SignOffSection.razor` are untouched (`git diff` shows zero changes there); the Admin `SignOff.razor` self-select page is untouched.
- **Reviewer-facing:** decisions (a)–(f) as written — re-minted `ar-deeplink` header token validated against the shared keyring + purpose; recipient-row cross-check (`OwnerType`, `WardStudentId`, `DeepLinkExpiresAt`); the guardian POST body carries no GuardianId; parallel public route group with error-map parity and no teacher-route churn; Families page token-bound with no self-select; certificate path via client + JS interop; redirect extension per (e) with student behavior unchanged; no new flag/keyring/purpose/packages/migration; best-practices + blazor rule compliance; test coverage list satisfied.
- **Orchestrator-facing:** a guardian deep link reaches the sign page without login and without any self-select; the sign POST is authorized server-side from the token (the body cannot impersonate a guardian); flipping `FEATURE:EnableDeepLinks` off dark-launches the guardian surface too; the existing suites remain green with no test edits beyond the planned additions.

### Residual risks / notes

- **`OwnerId` semantics on guardian recipient rows — CONFIRMED 2026-09-16 (parent, code-level):** `StudentsContactResolver` populates `owners.Add((ContactOwnerType.Guardian, g.GuardianId, g.StudentId))` from `StudentGuardianViewDto` (:52), so `AssignmentRecipient.OwnerId` IS the Guardian entity id for `OwnerType == Guardian` rows — the route may derive the acting `GuardianId` from it directly. Fail-CLOSED re-validation via `IsGuardianOfAsync` remains; the route test must assert the correct id threading.
- **Prod service-to-service auth gap — NARROWED but systemic gap REMAINS:** the guardian route group is public (token-gated), so Families' guardian calls need no OIDC `Authorization`; the stamp + ward-surface calls still 401 in prod (pre-existing ar-14 residual, D-6/deployment-auth workstream). Record, do not fix here.
- **Re-mint TTL posture:** header tokens are short-lived (suggested 15 min) and cannot outlive the stored `DeepLinkExpiresAt` (cross-checked server-side). The cookie principal itself is the ar-14 cookie posture (unbounded by the stored expiry until its own cookie lifetime lapses) — acceptable; noted for the D-6 identity round.
- **Bearer-token-in-header posture:** the re-minted header token transits the in-cluster HTTP connection (Aspire service discovery, HTTPS); same posture class as the landing URL token — recorded, mitigated by expiry + purpose scoping + the flag kill-switch.
- **Multi-ward guardian UX:** lands on `/ward`, navigates ward → assignment → sign-off; the direct sign-page hop for multi-ward guardians (targeting a specific ward from an out-of-band channel) is Phase-5 polish, recorded.
- **Antiforgery:** the guardian POST is an API call from the circuit's `HttpClient`, not a browser form post to the Families host — the ar-14 antiforgery note (GET-only public surfaces) still holds for the Families host; the API POST is header-token-gated. Worker should NOT add antiforgery to the API guardian group (it would break the client seam) — record if the reviewer raises it.
- **Two named skills do not exist**: `.github/skills/featureflags-tenant-gates/` and `.github/skills/fluentui-dialog-shell/` are absent (verified — `.github/skills/` holds only azure-ai-openai-dotnet, bounded-context, coded-values, dialog-ui, dotnet-best-practices, dropdown-ui, fluentui-component-props, fluentui-icons). The flag-gating posture is documented in `documents/configuration.md` §2 and dialog/section layout follows dialog-ui + the ar-9 page posture; flagged to the owner, not blocking.
- **Out of scope (record — do not touch):** E2/E3 delivery, D-6 identities, the systemic service-to-service credential fix, teacher UI (SignOffSection/ReassignSignerDialog/finalize), rubrics/comments, drawn signature, any change to the Admin-hosted `SignOff.razor` (the dev posture surface survives as-is).

## Prepared dispatch (parent — awaits owner go)

- **Worker** (`deepseek-v4-flash`): implement steps 1–7 on `stack/15-ar-15-signoff-relocation` cut from `feac20a4`; expected files list ±4; build-after-every-step; deviations reported, never silent; scope discipline per decision (f) — no E2/E3, no D-6, no teacher UI, no Admin `SignOff.razor` edits.
- **Reviewer** (`deepseek-v4.1-flash`, owner override): static diff-only verification against the binding decisions + acceptance criteria; check the no-self-select invariant, the no-GuardianId-in-body invariant, the recipient-row cross-check, error-map parity, and that teacher/admin surfaces show zero diff.
- **UI tester** (`minimax-m3`): bug-hunt the delivered Families guardian page (landing → ward → sign-off flow, consent gate, typed/click sign, already-signed re-entry, certificate affordance, LinkExpired path) — after worker + reviewer pass.
- **Orchestrator**: owns acceptance + the round-log updates (breakdown WS-F3 row, impl-details §3 ar-15 entry) post-verdict.

## Owner decisions on open questions (2026-09-16 — parent, post-plan review)

1. **`OwnerId` = GuardianId — CONFIRMED** (code: `StudentsContactResolver` `owners.Add((ContactOwnerType.Guardian, g.GuardianId, g.StudentId))`). The guardian route derives the acting `GuardianId` from `GetRecipientAsync(assignmentId, contactId).OwnerId`; the handler's fail-closed re-validation stays.
2. **15-minute re-mint TTL — ACCEPTED** for the header tokens (they cannot outlive the stored `DeepLinkExpiresAt` regardless — server-side cross-check).
3. **Missing skills — references dropped going forward** (`documents/configuration.md` §2/§5 carries the flag posture; dialog-ui covers layout). Installing them is an optional owner action outside this round.
4. **Multi-ward direct sign-page hop — CONFIRMED Phase-5 polish**; the round ships ward-list → assignment → sign-off navigation only.

**Dispatch gate:** owner go fires worker pass 1 (branch `stack/15-ar-15-signoff-relocation` cut from the `stack/14` tip `feac20a4`).

## Execution log (parent — Tier 3 round ar-15)

| Stage | Agent / model | Run | Outcome |
|---|---|---|---|
| Orchestrator-plan | `ollama-cloud/glm-5.3-flash` | `ce7b62ad` | First attempt **EARLY-STOPPED** (exit 0 after 90s, emitted only its opening line — no doc). **Revived** as `5d5f2418` → round doc authored complete |
| Worker pass 1 | `ollama/deepseek-v4-flash:0731-cloud` | `04ade4ee` | **TIMED OUT at the 30-min cap, NO report.** Landed the API-side partial: `Auth/GuardianTokenEndpointFilter.cs` + `Endpoints/GuardianSignOffRoutes.cs` (new), `Program.cs` wiring, the additive `GuardianSignOffRequest` contracts record, 2 new test files. Build **0 errors**, but `Assignments.Api.Tests.Unit` **74 total / 15 FAILED** — all 15 new guardian tests return **400 regardless of token validity** (happy path included) ⇒ the filter is not executing on the routes the tests hit. It crossed the cap fighting truncated MSTest output (its own log: "I've spent way too long… I cannot clearly see pass/fail due to tooling"). Families-side steps never started |
| Worker pass 1 (**ESCALATED**) | `ollama-cloud/deepseek-v4.1-flash` (escalator, owner override) | `1e1999ae` | *(in flight)* — reconcile-first, fix the filter/route seam to green, then continue the remaining steps |
| — | — | — | **Escalation clause invoked** (SKILL.md step 3): a timed-out worker pass is re-dispatched with the SAME scope on the escalation executor via a **write-capable** shell (`worker`), after a parent reconciliation. Under the owner's 2026-09-15 reviewer override the escalator is `ollama-cloud/deepseek-v4.1-flash`; **the escalated work must be statically re-verified by the HIGHER model `ollama/glm-5.3:cloud`** (not the standard reviewer) — owed at verification time |

**Parent reconciliation (2026-09-16, pre-escalation):** build 0 errors; suite 74/15 with the uniform-400 signature; root-cause hypothesis handed to the escalator (filter not applied to the tested routes, or the handler's `Items[...] is not Guid` guard 400s when it didn't run); test-output-discipline instructions added (single `grep -E "^\s*failed |total:|failed:"` command, no ad-hoc pipelines/debug logs, ≤2 iterations, 5-minute item cap). Untouched by the worker and left alone: the pre-existing uncommitted breakdown-doc edit, `brainstorms/`, `documents/specs/teachers-ward-portal-prefab-plan.md`.

**Post-escalation parent ground truth:** the escalation pass (timed out, no report) nevertheless COMPLETED the work — build 0 errors; full matrix **2,297/0** (Assignments.Api **75**, Families **31**); scope checks: `Assignments.Application` zero diff, shared-handler reuse, no body `GuardianId`, `GuardianTokenReMintTtl = 15 min`, Families page documents the no-self-select posture. Patch frozen: 15 files.

## Review (higher-model escalation verifier — `ollama/glm-5.3:cloud`, run `9acf2116`) — Verdict: **P1**

Mandated by the escalation clause (both worker passes timed out). Five P1s — **all invisible to the green matrix** because the route tests mock every handler and self-register their own JSON options:

1. **Host JSON converters missing** (`Assignments.Api/Program.cs` ~:19-38) — `SignatureTypeDto`/`SignOffStateDto` string-enum converters are absent in the real host, yet the Families client posts with generic enum JSON ⇒ the guardian POST fails `[FromBody]` binding with **400 in the real host**. The suite is green only because both new test files register the missing converters themselves.
2. **Token tenant not threaded to endpoint execution** (`GuardianTokenEndpointFilter.cs:79,138`) — the recipient cross-check runs inside `RunWithExplicitTenantAsync`, but `next(context)` is invoked AFTER that scope exits, so on the public group (no credential in prod) the tenant falls to Guid.Empty → the DB per-entity tenant query filters return nothing → every guardian GET/POST 404s in prod. Dev works only because TestAuth supplies an ambient tenant.
3. **Ward authorization is self-referential** (`GuardianTokenEndpointFilter.cs:112`) — the filter compares the TOKEN's `WardStudentId` to the route studentId, but the Families client mints that field FROM the page URL ⇒ URL-vs-URL; the loaded row's `WardStudentId` is never read and `GetSignOffContextQueryHandler` performs no caller/ward check ⇒ a guardian holding a valid recipient row for assignment A can read another student's sign-off context (name/score/pass/guardian names) and certificate PDF by editing the URL (POST stays fail-closed via `IsGuardianOfAsync`).
4. **Two planned test files missing** — `tests/SchoolCollab.Families.Tests.Unit/GuardianSignOffPageTests.cs` (bUnit) and `GuardianTokenHeaderTests.cs` (client re-mint/TTL/header) — the 315-line `Ward/SignOff.razor`, the client re-mint path and `GuardianCertificateDownloadService` are new behavioural code with **zero tests** (AGENTS.md pre-flight rule).
5. **Ward-surface "Sign off" affordance missing** — no route in the ward surface links to `/ward/{sid}/assignments/{aid}/sign-off`, so decision (e)'s second clause is dead: a multi-ward guardian (token `WardStudentId` null → lands on `/ward`) has **no UI path** to the sign page.

**P2s:** `documents/configuration.md` §5/Introduced-flags rows still describe only the ar-14 landing/mint (no WS-F3 guardian gating note) · pre-existing teacher-POST converter seam (`AssignmentsApiClient` registers the converters client-side while the host block lacks them) — corroborates P1-1 and is fixed by item 1 · `SignOff.razor:165` replaces `_loadCts` without cancelling/disposing the prior CTS (stale-continuation race + leak) · `GuardianCertificateDownloadService` is DI-scoped but disposed as component-owned (transient would match the documented consumer-owned pattern).

**Best-practices:** no overwrites (the two Families ward-test edits are compile-required ctor-arg registrations — proportionate); skills/rules honored (bounded-context boundary held, endpoint grouping, no MediatR, primary ctors + XML docs, no new package/flag/migration/keyring/purpose, no inline styles, CPM/net10.0 intact, `fileDownload.js` a verbatim copy).

**Decisions (a)–(f):** (a) mostly conforms — deviations are P1-2/P1-3; (b) conforms (no body `GuardianId`, fail-closed `is not Guid → 401`, no self-select anywhere, Admin page untouched); (c) conforms (same handlers; teacher surfaces zero diff); (d) conforms (contracts diff = exactly one additive record; error-map parity verified incl. identical certificate stream name; reassign/finalize absent and asserted); (e) **partially** — redirect implemented + tested, but the ward-surface affordance is missing (P1-5); (f) conforms.

### Parent adjudication (owner decision for P1-3)

Authorize the guardian **GET context** and **certificate** paths via the **guardian link** for the route student (reuse the existing `IsGuardianOfAsync`/equivalent — the token's ContactId must be a guardian of the route studentId), keeping the recipient row for (a) "is a recipient of this assignment" and (b) the stored `DeepLinkExpiresAt` cross-check. **Do NOT** use `row.WardStudentId == routeStudentId`: the row is `FirstOrDefault` per (assignment, contact), so its `WardStudentId` is the *first-resolved* ward and that predicate would 403 legitimate multi-ward sign-offs. POST keeps the handler's fail-closed check. Tests must prove: a guardian of ward B cannot read ward C's context/certificate, and a multi-ward guardian CAN read both of their wards.

**Rework pass dispatched** (reviewer iteration 1 of ≤2).

### Rework pass 2 + parent completion (2026-09-16)

Rework pass 2 (`ee3b1321`, worker model) landed review items **1–6** (host JSON converters, tenant-scope wrap around `next(context)`, guardian-link authorization per the adjudication, both missing test files, the ward-surface "Sign off" affordance, the `configuration.md` WS-F3 rows) plus P2s **7–8** (`SignOff.razor` `_loadCts` `Cancel()`+`Dispose()` before replacement; `GuardianCertificateDownloadService` registered `AddTransient`). It was then **interrupted** after launching a forbidden filesystem-wide `find`; the parent completed the remainder directly rather than dispatching a fourth worker.

**Parent-completed: the two failing bUnit tests**, whose root cause was in the *test*, not the page (the page's `@bind-Value` FluentUI usage is correct):

- The worker's MockHttp matchers used the **method-agnostic** `When(url)` for the context. MockHttp returns the *first* matching matcher, so that matcher swallowed the sign **POST** and both GETs — the page never left the awaiting view. Fixed: method-specific matchers (`When(HttpMethod.Get, …)` / `When(HttpMethod.Post, …)`) + a counter-based awaiting→signed GET sequence (`SetupContextThenSignedAfterSign`, `Interlocked` counter, fresh per MSTest instance).
- The tests synthesised FluentUI web-component events with base `ChangeEventArgs` (`oncheckedchange`/`onradiogroupchange`), which FluentUI cannot convert to `CheckboxChangeEventArgs`; the exception surfaced through the page's `ErrorBoundary`. Fixed: drive the **bound callbacks** (`cut.InvokeAsync(() => cut.FindComponent<FluentCheckbox>().Instance.ValueChanged.InvokeAsync(true))`, same for `FluentTextField` / `FluentRadioGroup<SignatureTypeDto>`), the repo's bUnit pattern.
- Removed the worker's leftover debug markup dump (`File.WriteAllText(..."pi-sign-markup.html")`).

### Review r2 (higher-model re-verification — `clinepass/cline-pass/glm-5.3`, synchronous) — Verdict: **P2-only**

Escalation-clause re-verification of the escalated/reworked work. **All 5 P1s and 4 P2s RESOLVED**, each with on-disk evidence (converters at `Assignments.Api/Program.cs:46-47`; `next(context)` at `GuardianTokenEndpointFilter.cs:157` inside `RunWithExplicitTenantAsync` opened at :99, with the recipient lookup, tenant/owner-type/guardian-link (:136) and stored-expiry (:146) checks all inside the scope; authorization via `IsGuardianOfAsync(routeStudentId, recipient.OwnerId)` with **no** `WardStudentId == routeStudentId` predicate and tests proving cross-ward denial **and** multi-ward access where the mocked row's ward differs from both route wards; both test files present and covering the plan's binding list; the affordance at `Ward/Index.razor:57-65` with a bUnit assertion). **Test-fix integrity: PRESERVED** — both sign flows genuinely exercise the guardian POST + reload, no debug dump remains, and no assertion became vacuous.

One P2 raised: `GuardianSignOffPageTests.cs` closed with a vacuous `_mockHttp.Should().NotBeNull()` presented as the proof that the POST body carries no `GuardianId`.

**Parent P2 fix (both reviewers' findings now closed):** added `SetupSignOkCapture()`, which captures the POST body the page actually issued, and replaced the vacuous line with `_signBody.Should().NotBeNullOrWhiteSpace(...)` + `_signBody.Should().NotContainEquivalentOf("guardianId")` — so the typed-sign test now *proves* decision (b) rather than asserting the claim in a comment (the structural proof — the request record has no such member — and the server-side proof in `GuardianSignOffRoutesTests` remain).

### Final ground truth (post-rework, post-P2-fix)

`dotnet build SchoolCollab.sln` **0 errors**; full matrix **2,311 / 0** — Core 84, Assignments 597, **Assignments.Api 78**, **Families 42**, Students 422, Students.Api 1, Settings 519, Settings.Api 1, Admin 546, ArchitectureTests 21. Patch re-frozen: **19 files / 2,063 insertions**. UI tester dispatched on `clinepass/cline-pass/minimax-m3` (run `adddcbb6`).

### Provider directive (owner, 2026-09-16)

All agent roles in this workflow now dispatch on the **`clinepass`** provider (`clinepass/cline-pass/<id>`); the `ollama/…` ladder is legacy and appears above only because this round started under it. Ladder recorded in `.pi/skills/orchestrator-worker-reviewer/` (`SKILL.md` + `references/models.md`): orchestrator `glm-5.3-flash`, worker `deepseek-v4-flash-0731`, reviewer `deepseek-v4.1-flash`, UI tester `minimax-m3`, higher-model re-verify `glm-5.3`.

### Process note (for the round skill)

This round burned two 30-min worker timeouts, one escalation, one runaway `find`, and a parent completion. Two durable lessons: (1) the worker's own log blamed *truncated MSTest output*, not the code — the skill's strict single-grep test-output rule needs to be stated as a hard gate, not advice; (2) a MockHttp matcher that omits the HTTP method silently shadows a later method-specific matcher (first-match wins) — a green-looking bUnit test can therefore never exercise the call it is named for.

### UI tester (`clinepass/cline-pass/minimax-m3`, run `adddcbb6`) — Verdict: **NITS** (one P2 a11y)

**Applied parent-side (smallest fixes):**

1. **a11y (the P2).** The consent checkbox and the typed-signature field each paired a visible `Label` with a *differently worded* `aria-label`, so the exposed accessible name could disagree with the visible text (WCAG 2.5.3 label-in-name). Aligned each `aria-label` to its visible label text. Kept (not dropped) deliberately: the tester could not confirm statically that FluentUI 4.14.2 wires `Label` into the accessible name, and a matching aria-label is correct in **either** case, whereas dropping it would regress a11y if `Label` is ignored.
2. **Failure-state navigation.** The “Back to ward” anchor lived inside the success branch, so an expired / revoked / failed load rendered only the error bar with no in-page way back. Hoisted to the top of `ChildContent` so it renders in every state.

**Residuals (recorded, non-blocking — deliberately NOT fixed in this round):**

- The `Label` + `aria-label` doubling is **pre-existing repo-wide**: the teacher/Admin surface this page was relocated from (`Assignments.Application/…/SignOff.razor:77-78,115-116`) carries the identical pair, and the repo's dominant convention (`Create.razor`, `Edit.razor`, `PublishDialog.razor`) is `Label`-only. Harmonizing — dropping the redundant attribute on **both** surfaces — is a follow-up; the Admin surface is a zero-diff invariant of this round and a one-sided change would make the relocated page diverge from the page it replaced.
- `Describe(ex)` is shared by the **load**, **submit**, and **certificate** paths, so a blanket `404 → “certificate not available”` would mislead on the first two; the correct fix is a certificate-local mapping (per-call-site wrapper).
- The new “Sign off” `TemplateColumn Title=""` matches the file's own convention — the pre-existing “Open” column is also `Title=""` — so the blank header is consistency, not a defect.
- Typed→Click switch retains `_typedSignature` (minor; the user retypes).
- The Sign button's label (“Sign” / “Click to sign”) duplicates the radio option text (“Click to sign”); a constant label would churn the two bUnit assertions that locate the button by its text.

**Not-verifiable-statically (recorded):** `fileDownload.js` runtime save path; whether the ar-14 cookie principal's `contact_id`/`tenant_id` claims are populated when the page is reached via the landing redirect; whether the in-cluster discovery transport preserves the `x-deeplink-token` header; whether FluentUI 4.14.2 wires `Label` to the accessible name.

### Round close

**CLOSED 2026-09-16.** Build 0 errors; authoritative matrix **2,311 / 0** (Core 84, Assignments 597, Assignments.Api 78, Families 42, Students 422, Students.Api 1, Settings 519, Settings.Api 1, Admin 546, ArchitectureTests 21); patch 19 files / +2,063. Review chain: higher-model **P1 ×5** → rework → re-verify **P2-only** → parent P2 fix → UI tester **NITS** → parent a11y/nav fixes. Round is ready to commit and submit as the third stacked PR (delegated to the commit agent on the owner's go).